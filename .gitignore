# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
*.egg-info/

# Node / Next.js
node_modules/
.next/
out/
playwright-report/
test-results/
.playwright-mcp/

# Env & secrets — never committed
.env
.env.*
!.env.example

# Claude Code — personal/local files stay out; shared config stays in
.claude/settings.local.json
CLAUDE.local.md

# Data & artifacts
*.sqlite
tarifhub_dev.db
data/raw/
site/
.shipboard/
# PDF build workspace (tarifhub-cas.pdf is reproducible via tools/build_pdf.py)
build/
# Local-only PDF builder: kept out of the repo and Pages (lecturer scope = repo + Pages only).
# Restored locally so `python3 tools/build_pdf.py` works on this machine; never committed.
tools/build_pdf.py
docs/latex_template/

# OS / editors
.DS_Store
.idea/
.vscode/
.gstack/
.claude/agent-memory/
.claude/agent-memory-local/
services/*/.claude/
vault/.brain_sync_last

# Knowledge-tooling workspace state (Obsidian, graphify)
# .graphifyignore is deliberately TRACKED (single source of truth for graph
# exclusions — see tools/hooks/README.md)
.obsidian/
graphify-out/
.claude/worktrees/
# Eraser MCP render scratchpad (diagram deliverables are committed under docs/img/diagrams/)
.eraser/
docs/cas/bewertungskriterien-anker.md

vault/00-index.md

vault/cas-audit/

vault/cas-audit/

# Superseded brand iterations
docs/brand/assets/_legacy_v4/
docs/brand/assets/_legacy_v5/
