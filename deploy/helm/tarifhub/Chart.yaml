apiVersion: v2
name: tarifhub
description: >-
  tarifhub — AI-assisted Swiss ambulatory tariff harmonization. Deploys the Python
  ingestion service (pre-freeze) and the FastAPI serving service (deterministic,
  read-only), with an optional in-cluster PostgreSQL + pgvector.
type: application
version: 0.1.0
appVersion: "0.1.0"
keywords:
  - tarifhub
  - healthcare
  - tariffs
  - switzerland
home: https://github.com/tarifhub/tarifhub
maintainers:
  - name: tarifhub
