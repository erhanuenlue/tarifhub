# 07 — Documentation, Fazit, submission package (Block 3)

> **Gate-01: pre-approved by the owner (13 Jun).** Produce and log the plan as usual (emit + plan report), then proceed without waiting for approval. STOP and ask only for: scope beyond this prompt, any freeze-line contact, a green-contract or ratchet breach, or a destructive operation.

Read AGENTS.md and CLAUDE.md. Final assembly: turn five weeks of evidence into the artifact the grade is read from. The PDF is the product of this session.

**Anchor rule for this session (official Bewertungskriterien):** levels are conjunctive — every element of a criterion's top anchor must be present or the whole tier drops. The two big criteria below are written **to their anchors**, not to taste. A further criterion-4 anchor governs the whole assembly: the report must carry a **durchgängiger roter Faden** — Problemstellung → Lösungsansatz → Fazit, readable by a third party; weave it, then verify it by reading the chapter openings in sequence.

1. **AI-tools chapter** (criterion 15 — 12 points, the largest single criterion). The top anchor dictates the structure — **organise by phase, not by tool**: *Generierung* (implementers, scaffolds, ai_map prompt design) · *Review* (verifier, determinism-auditor, security, **Codex as independent second model family**) · *Refactoring* (fix cycles, the cross-engine drift fixes) · *Recherche* (Explore agents, FHIR IG analysis, Context7). Within each phase: which tools, how orchestrated (the /ship pipeline, hooks as *governance over the AI by the AI's own tooling*, `CLAUDE.md`/`AGENTS.md` as reproducible context), and **evidence per claim — prompts, diffs, commit refs** from the journal and PR history; at least six worked examples including two failures and what caught them. Close the chapter with the **Erklärung der Eigenständigkeit** slot: reserve the page (standard academic form as a visible placeholder with name/place/date line) but **Erhan authors the final text himself** (owner decision 13 Jun). Tell him explicitly in the ship report that this page awaits his words before submission.
2. **The Fazit** (criterion 18) — built on the anchor's own template. Core: **three veto decisions — "nie an die KI delegiert" — each concrete, justified, and evidenced**: ① no AI ever computes or mutates a billing value (the freeze line; evidence: the `guard_frozen` block event of 11 Jun and the owner-authorized one-line audit fix) ② plan approval and the merge green-contract stay under human judgment (evidence: gate-01 corrections, the dispositioned-findings record) ③ the journal and this Fazit are written in my own voice, never generated (evidence: the curation diffs). Around the vetoes: what *was* delegated and how it went · measured observations (review burden, re-version churn, recall@5 .833→.917, where agentic beat spec-driven and the reverse) · the central lesson (an enforced, tested determinism boundary is what makes AI usable on billing data) · honest limits · **the transfer: how this changes my future working practice** (the anchor's closing element) · two course-literature references. 1.5–2 pages, first person, no triumphalism.
3. **German insurance:** a one-page **deutsche Zusammenfassung** (vision, architecture, AI usage, Fazit-Kernaussage) after the cover, and the Fazit's key paragraph in both languages.
4. **Site + PDF (owner-specified, 13 Jun):** the site stays GitHub Pages (mkdocs build --strict clean). The **submission PDF is built from the official FFHS LaTeX template** at `docs/latex_template/` (`ffhsthesis.cls` + logo): assemble the arc42 chapters, the AI-tools chapter, **journal excerpts and the Fazit as chapters inside the document**, the criterion map (CAS Dossier §6 table, final state) as an appendix, and the Eigenständigkeitserklärung page; front matter per the class (dokumentTyp "Projektarbeit CAS AISE", Studiengang, title, author), **repo URL clickable on the title/intro page**. Pipeline: pandoc markdown→LaTeX into the class (install tectonic or BasicTeX as needed; if the toolchain fights for more than ~30 minutes, fall back to the designed HTML→PDF route in the owner's Convergence patterns and say so in the report — the content, not the engine, is graded).
5. **Style law (owner, 13 Jun): no em-dashes anywhere in the documentation.** Sweep `docs/arc42/`, `docs/method/` and every PDF-bound text: replace each em-dash with a comma, colon, period, or parentheses as the sentence demands (rewrite, don't just substitute). Verify with a grep; zero is the target.
6. **Pre-flight** (CAS Dossier §8): run every item, report each with evidence, fix what fails. Repo reachable in incognito, gitleaks green, CI green on the submission commit, README cold-start credible, **em-dash grep returns zero**, the PDF compiles from the FFHS template and renders correctly.

Constraints: no code changes this session except what pre-flight forces. The Fazit's honesty is its value — a documented 20% review rate beats a hidden one; an AI failure analysed beats a success asserted.

Submission mechanics (confirmed on the Abgabe page, 12 Jun): assignment *"Projektarbeit: Deployment & Abgabe"*, submitted as **Gruppe K**, due **Mon 6 July 2026, 00:00** — PDF upload containing the clickable repo URL. The arc42 §8 page opens with the refreshed criterion-8 wording verbatim (*"Konzepte des gewählten Frameworks und moderner Applikationsentwicklung sachgerecht eingesetzt"* — stack-neutral since the 12 Jun rubric refresh); claim the 10 points directly, no hedging.

Done means: the PDF is uploaded to Moodle and renders; the repo is frozen on the submitted commit; `/cas-status` reports every floor item green. Then stop. Curate the final journal entry — and take the evening off.
