# 10 · Presentation deck (Reveal.js, 5-minute talk)

> **Gate-01: pre-approved by the owner (14 Jun).** Produce and log the plan (emit + plan report), then proceed without waiting. STOP only for: scope beyond this prompt, any freeze-line contact, a green-contract or ratchet breach, or a destructive operation.

Read AGENTS.md, CLAUDE.md, and the AI-SE framework chapter (`docs/`, the chapter prompt 08 produced). Run at `/effort ultracode`. Build a short, minimalist Reveal.js slide deck for a **5-minute live presentation at the school** about the TarifHub idea and how it was engineered with the auto-loop. This is a talk aid, not a document: few words per slide, one idea per slide, the speaker fills the rest.

## Output

A single self-contained file `docs/presentation/index.html` using Reveal.js. Prefer the Reveal CDN; if offline rendering matters, vendor the minimal Reveal assets under `docs/presentation/`. Minimalist design: one clean theme (white or near-black background, a single accent colour matching the brand tokens in `docs/brand/tokens.css`), large type, generous whitespace, no clipart, no transitions beyond a simple fade. Reuse the Eraser diagrams already in `docs/img/diagrams/` (do not regenerate them) where a slide benefits from one. Speaker notes on each slide (Reveal `aside class="notes"`) with the talking points and a rough time budget, so the 5 minutes are paced.

## Story arc (about 9 to 11 slides, 5 minutes)

1. **Title:** TarifHub, one line on what it is (Swiss ambulatory tariff data, made trustworthy), name, date.
2. **The problem:** why tariff data is hard and why trust (determinism) is the point. One sentence, one visual.
3. **The idea:** the four-layer architecture in one diagram (reuse the Eraser four-layer image), the freeze line as the trust guarantee.
4. **How I started:** an empty repo plus a definition of done, not a backlog; outcome prompts, not scripts.
5. **How it was developed:** the /ship multi-model pipeline (reuse the pipeline diagram); orchestrator plans, workers build, an independent model reviews.
6. **Auto-loop engineering:** the headline. Fixed loop vs self-prompting loop (reuse the loop diagram); the machine writes its own next step and runs unattended.
7. **The gates that make autonomy safe:** the fitness ratchet, the freeze-line guard, the green-contract auto-merge, the secret gate (reuse the gates diagram). This is the intellectual core, give it a clear slide.
8. **Quality and CI/CD:** every PR reviewed by a second model family, full CI, self-contained docs and diagrams generated by the loop.
9. **The dashboard:** one screenshot of Shipboard (capture the Overview or Loop tab into `docs/img/` if no shot exists), the system is observable end to end.
10. **What I learned / transfer:** an enforced, tested definition of done is what makes AI autonomy trustworthy; the method is now a reusable framework.
11. **Close:** repo link, docs site link, one-line thank-you.

Keep total spoken content to about 5 minutes: that is roughly 30 to 40 seconds per slide, so each slide is a headline plus at most three short bullets, detail lives in the speaker notes.

## Steps

1. Build `docs/presentation/index.html` per the arc and design above, pulling real facts from the repo and the framework chapter (no invented numbers; use the true ones: PRs merged, CAS floor, recall figures, the model and effort map).
2. Capture a Shipboard screenshot into `docs/img/` if one is needed for slide 9 (start the board, screenshot, stop it); otherwise reuse an existing image.
3. Open the file headlessly to confirm it renders (no console errors, slides present, images resolve). Quote the slide count.
4. Optionally publish it with the docs site (link from the docs index) so there is a URL, but the local file is the deliverable.
5. Style law: no em-dashes in slides or notes; grep zero.
6. Pre-flight: file opens, all referenced images exist, slide count matches the arc, speaker notes present. Then `/ship` (codex-reviewer sanity-checks the deck content and timing).

Constraints: presentation asset only, no product code, no freeze-line files. Conventional commit, branch `docs/presentation`, `/ship`; phase 09 auto-merges on the green-contract, else stops for the owner.

Done means: `docs/presentation/index.html` is merged, renders as a minimalist Reveal.js deck of about 9 to 11 slides with speaker notes and a 5-minute pacing, reuses the Eraser diagrams, em-dash-free. Tell me in the report how to open it (the file path and, if published, the URL) and the per-slide time budget. Curate the journal entry.
