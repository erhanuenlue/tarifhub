# 06 — Validation & distribution evidence (Block 2)

Read AGENTS.md and CLAUDE.md. This session produces the artifacts criteria 13, 14 and 17 score — the points teams routinely leave on the table. Almost no new features; maximum legibility.

**Anchor rule that governs this whole session (official Bewertungskriterien):** levels are conjunctive — a top level is awarded only if *every* anchor element is present. For criterion 14 the anchors say it outright: *a screenshot or an unbacked "alle Tests grün" claim is the 1-point level*; full marks require **pipeline output, documented and interpreted in the report**. Screenshots illustrate; quoted output + interpretation score.

1. **Coverage to target:** fill unit-test gaps on core modules (model, freeze, pipeline, mapper, serving routes) to >80% line coverage. No test theater — each new test asserts behaviour, not implementation (crit. 13's anchor: core logic **incl. error cases**, running in the build).
2. **Test-results artifact** (`docs/arc42/10-quality-requirements.md`, "Results" section) — built to crit. 14's top anchor: **quoted pipeline output** (the pytest summary lines and the CI job excerpt with the determinism boundary test visible) **plus an interpretation paragraph each** (what the numbers mean, what they prove, what they exclude); the coverage figures as tool output; the harmonisation report table (both sources: in/out, review rate, confidence); one diff-query output example; the §10 NFR table with a "measured" column — each NfA showing **Zielwert · Messverfahren · measured value · ADR-ref** (crit. 2's anchors). CI link + screenshot stay, but as illustration, never as the evidence itself.
3. **Acceptance-criteria cross-refs** (crit. 11's anchor): every Given/When/Then in §10 gains its explicit NfA reference ("verifies NfA-3, determinism") — one editing pass, no new content.
4. **Test-strategy AI subsection** (crit. 12's anchor — *"Tests der KI-Anteile berücksichtigt"*): one explicit subsection in §13 naming the AI-portion tests as such — the AST boundary test (guardrail behaviour), the gap-gate tests (no-call paths), the fill-reuse determinism tests (handling non-deterministic AI output), the schema-validation fail-closed tests. They all exist; this names them against the criterion's own words.
5. **Distribution evidence** (criterion 17, code-only review): verify every sub-system image builds in CI (`docker build` matrix); capture one `docker compose up` + `docker ps` screenshot showing the independent containers; optionally one local k3d `helm install` + `kubectl get pods` screenshot. Embed both in the deployment chapter with the Helm chart structure explained in six lines **plus the style-choice justification sentence (ADR-002/006) the anchor demands**.
6. **Decision matrix** (`vault/decision-matrix.md` → also into the docs AI chapter): fill the skeleton — Vibe vs Spec-Driven vs Agentic against this project's constraints, each cell grounded in a dated journal incident or a course-literature reference ([1] Osmani, [9] O'Reilly). Conclusion argued, not asserted.

Constraints: nothing below the freeze line; no new endpoints; the screenshots go into the repo (`docs/img/`) so the site and PDF are self-contained.

Done means: a grader can verify criteria 13/14/17 without leaving the PDF. Verifier pass framed as that grader. `/ship`; journal curated.
