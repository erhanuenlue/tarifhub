---
title: 2026-06-07
type: daily
date: 2026-06-07
tags: [daily]
---

# 2026-06-07

## Done
- Wired the Claude Code dev setup into the repo: `.mcp.json` (context7, graphify, obsidian),
  extended PreToolUse guard + Stop/SubagentStop determinism hooks, three subagents
  (codex-reviewer, determinism-auditor, security-reviewer), four slash commands
  (`/ship`, `/review-codex`, `/new-source`, `/arc42-update`), and the `scripts/` flow
  (bootstrap-github, ship, setup-claude-code).
- Scaffolded this Obsidian vault and recorded the workflow decision
  ([[decisions/0001-adopt-claude-code-ultracode-codex-review]]).

## Next
- On my machine: run `scripts/setup-claude-code.sh` (Codex CLI + codex-plugin-cc + graphify +
  skills), then `gh auth login`, then `scripts/bootstrap-github.sh` to create the private repo
  + branch protection.
- First real change: try `/ship` end-to-end on a tiny edit to confirm the Codex-review-before-merge
  loop and branch protection behave.
- Build the graph: `/graphify .` (or let the post-commit hook do it), then
  `graphify --obsidian knowledge`.

## Open questions
- Confirm the exact codex-plugin-cc + mattpocock/skills install names against upstream READMEs.
- Decide whether to add a read-only Postgres MCP for the serving/survey data (kept out for now).

## Determinism watch
- Nothing touched the freeze line today. Guard now also covers the TarifIQ crosswalk and
  `rules_frozen*`.
