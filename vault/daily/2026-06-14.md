# 2026-06-14: CAS doc correctness pass, 3 report-vs-repo contradictions

I fixed three self-contradictions an independent review found between the submission docs and the repo. The change shipped through PR #23, squash-merged as `163eda7 docs(cas): fix three report-vs-repo contradictions (console tests, fhir_parser, Quarkus env) (#23)`. Scope stayed in docs and `.env.example`: no `services/` edits and no freeze-line changes. `cas_check` stayed 63/63, MkDocs `--strict` was clean, and the PDF was rebuilt without em dashes.

Today's commits:
- `e6deb5d vault: codex second opinion on grade estimate [skip ci]`
- `7d0764a chore(vault): journal + index (auto, session end) [skip ci]`
- `3143c39 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `5cb8cb1 chore(vault): journal + index (auto, session end) [skip ci]`
- `163eda7 docs(cas): fix three report-vs-repo contradictions (console tests, fhir_parser, Quarkus env) (#23)`
- `98678d4 vault: curate journal 2026-06-13 via gpt-5.5 [skip ci]`
- `8c2d551 docs: cas correctness-fix prompt`
- `2dfb719 chore(cockpit): design package under docs/cockpit, excluded from build (deferred post-submission, extract per ADR-022)`
- `926cdaf Update gitignore`
- `c0b56e4 chore(vault): untrack auto-generated 00-index.md (brain_sync rebuilds it locally)`
- `cbfa108 vault: curate journal 2026-06-13 via gpt-5.5 [skip ci]`
- `72007b4 vault: codex second opinion on grade estimate [skip ci]`

## Delegated AI work

I delegated review to the verifier, Opus in fresh-context diff-vs-spec mode, and to the codex-reviewer, gpt-5.5 as the independent second model family, on every PR round.

## Accepted, corrected and rejected AI suggestions

Accepted: the verifier caught the green overclaim in my first pass. The CI `console` job actually runs `1 passed / 1 skipped` because the second Playwright spec is `test.skip(!CAPTURE,...)`, a screenshot helper. I corrected the documentation instead of claiming two green Playwright end-to-end tests.

Accepted: codex gpt-5.5 independently caught the same class of errors and more: the phantom `parsers/fhir_parser.py` in the C4 component diagram embedded in arc42 §5, the same phantom in UC-08, the `typecheck` overclaim because CI runs `npm ci`, lint, build and test but never `npm run typecheck`, the parser-pairing error because `_PARSERS` maps `bag_eal` to `bag_eal`, not to `bag_eal` plus `xlsx_parser`, and that "green on latest main" was unbacked.

Accepted: round-2 codex caught about 10 em dashes in the regenerated diagram SVG. I removed them to keep the owner law for documentation, report and PDF-bound text.

Corrected: one of my own correctness claims was wrong. I wrote that until 2026-06-14 no test script existed. `git show 265b2e3:apps/tarifguard/package.json` proved the `test` script shipped with the console on 2026-06-13 in PR #16. I reframed the issue as stale docs from day one.

Accepted: for criterion 14, I replaced an unquoted green claim with real pipeline output from `gh run view --log` in arc42/13: `Tests 18 passed (18)` and `Running 2 tests / 1 skipped / 1 passed (17.2s)`. I interpreted it as 18 Vitest tests passing, one e2e smoke passing, and one CAPTURE-gated screenshot spec skipped. This is quoted and interpreted pipeline output, not a screenshot as evidence.

Rejected for this task scope: codex flagged real P1s in `services/`, including `:8080` defaults in mcp/intelligence and a `fhir_parser` reference in ingestion/README, plus a pre-existing review-endpoint contradiction. I did not fix them because the prompt forbade `services/` edits and they were a fourth contradiction beyond the three named. I flagged them in the PR and report for a separately authorized task, which would also need determinism-auditor.

## Build detail

`tools/build_pdf.py` `convert_svg()` caches SVG-to-PDF by existence only with `if not out.exists()`. After editing a diagram, I had to remove `build/pdf/img/<name>.pdf` before rebuilding, otherwise the stale render would ship. I verified the fix by checking that the rebuilt PDF text had 0 `fhir_parser` hits.

<!-- session ended 12:06 · branch: main · last commit: 163eda7 docs(cas): fix three report-vs-repo contradictions (console tests, fhir_parser, Quarkus env) (#23) -->

<!-- session ended 12:15 · branch: main · last commit: 3143c39 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci] -->
