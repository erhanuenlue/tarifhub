# 2026-06-14: CAS documentation correctness, English thesis, C5 interface contract, and zero German PDF-bound docs

I fixed three self-contradictions an independent review found between the submission docs and the repo. The change shipped through PR #23, squash-merged as `163eda7 docs(cas): fix three report-vs-repo contradictions (console tests, fhir_parser, Quarkus env) (#23)`. Scope stayed in docs and `.env.example`: no `services/` edits and no freeze-line changes. `cas_check` stayed 63/63, MkDocs `--strict` was clean, and the PDF was rebuilt without em dashes.

AI suggestion accepted: I accepted the verifier's correction that my first pass overclaimed the console CI result. The CI `console` job runs `1 passed / 1 skipped`, because the second Playwright spec is `test.skip(!CAPTURE,...)`, a screenshot helper. I corrected the documentation instead of claiming two green Playwright end-to-end tests.

AI suggestion accepted: I accepted codex gpt-5.5 findings on the same contradiction class and additional ones: the phantom `parsers/fhir_parser.py` in the C4 component diagram embedded in arc42 §5, the same phantom in UC-08, the `typecheck` overclaim because CI runs `npm ci`, lint, build and test but not `npm run typecheck`, the parser-pairing error because `_PARSERS` maps `bag_eal` to `bag_eal`, not to `bag_eal` plus `xlsx_parser`, and that "green on latest main" was unbacked.

AI suggestion accepted: I accepted the criterion 14 correction. I replaced an unquoted green claim with quoted pipeline output from `gh run view --log` in arc42/13: `Tests 18 passed (18)` and `Running 2 tests / 1 skipped / 1 passed (17.2s)`. I interpreted it as 18 Vitest tests passing, one e2e smoke passing, and one CAPTURE-gated screenshot spec skipped.

AI suggestion rejected: I rejected codex findings as outside the PR #23 task scope where they required `services/` edits or named a fourth contradiction beyond the three authorized ones. These included `:8080` defaults in mcp/intelligence, a `fhir_parser` reference in ingestion/README, and a pre-existing review-endpoint contradiction. I flagged them in the PR and report for a separately authorized task, which would also need determinism-auditor.

I applied the owner decision from 14 Jun that the CAS thesis must read as a fully English document. PR #24 squash-merged as `c4b66f9 docs: make the CAS thesis fully English (#24)`. Scope stayed in `docs/` and `tools/build_pdf.py`: no `services/`, no freeze-line files, and `tools/cas_check.py` / `tools/cas_baseline.json` were untouched. `cas_check` held 63/63 throughout, the German rubric anchors were preserved verbatim and glossed in English by design, MkDocs `--strict` exited 0, full CI was green, the PDF was rebuilt at 100 pages, and the thesis prose had zero em dashes.

For PR #24 I used a 26-agent Workflow fan-out: one Opus worker per doc, translate then fresh-context verify. The orchestrator, Opus 4.8, kept the LaTeX change limited to redefining `\maketitle` and `\titlehead` in the build preamble so the five cover literals hardcoded in the official `ffhsthesis.cls` render in English: "im Studiengang", "von", "Eingereicht bei:", the running-head "Studiengang", and the "Referent" label. I did not edit the class file. I accepted the resulting cover change after reading the rendered PDF pages, which showed "Submitted to:", "Advisor:", "by", "in the study programme CAS AI-Assisted Software Engineering", and "Switzerland, 2026".

Correction: I corrected my own claim that no test script existed until 2026-06-14. `git show 265b2e3:apps/tarifguard/package.json` proved the `test` script shipped with the console on 2026-06-13 in PR #16, so I reframed the issue as stale docs from day one.

AI suggestion accepted: I accepted round-2 codex feedback that the regenerated diagram SVG still contained about 10 em dashes. I removed them to keep the owner law for documentation, report and PDF-bound text.

Correction: I corrected my own German scan for PR #24. My umlaut-plus-function-word heuristic missed `docs/method/ai-se-framework.md` and `decision-matrix.md`, because words such as "Generierung", "Recherche", and "Fazit" carried no umlauts and were not in my wordlist. The verifier passed because it only checked diffed files, while codex gpt-5.5 crawled the whole repo and flagged them. Codex also flagged four wording nits: "equally valid to" to "as", "released data" to "frozen data", the phase list, and "List of Resources" to "List of Aids and Tools". I fixed these in `437764b`; codex re-review returned LGTM.

AI suggestion accepted: I accepted the per-file verify stage findings from the translation workflow. It flagged glossed-anchor leftovers such as "(Problemstellung)", "(Fehlerbehandlung)", the connective "je", and "nie an die KI delegiert". The orchestrator removed them so that only the `cas_check` rubric anchors remained German.

AI suggestion rejected: I rejected full AI translation of the official German rubric quotations in PR #24 as a judgment call. I kept criterion-8/12/18 quotations such as „...als Fazit reflektiert", „gewählten Frameworks", and „Tests der KI-Anteile" verbatim as citations and glossed them in English, because I treated them as original grading-rubric quotations. The owner overturned this in PR #26.

I acted on the independent reviewer's criterion-5 finding and added an explicit interface-contract view to the interaction perspective. PR #25 squash-merged as `cebed36 docs(arc42): add interface-contract view to the interaction perspective (C5) (#25)`. I added arc42/06 section 6.5 for the L1 serving surface, REST, FHIR R4 and MCP, as a tested contract with `as_of`, versioning and diff semantics, a pointer to the generated OpenAPI document and `services/serving/tests/test_openapi.py`, and the determinism guarantee stated at the interface level. I committed one Eraser sequence diagram as PNG plus `.eraser` source and repointed the criterion-map C5 interaction row from the use-case diagram to this view. Docs only: no `services/`, no freeze-line, `tools/cas_check.py` and `cas_baseline.json` untouched. `cas_check` held 63/63 with criterion 5 = 4/4, MkDocs `--strict` was clean, em-dash grep was 0, the PDF was rebuilt with the new section and diagram, and full CI was green on the PR and on main including the images job.

For PR #25 I delegated review to the verifier, Opus in fresh-context diff-vs-spec mode, and the codex-reviewer, gpt-5.5 as the independent second family. The verifier returned PASS. Codex round 1 used 145k tokens. Codex round 2 used 58.8k tokens.

AI suggestion accepted: I accepted codex round 1 findings for PR #25 after it opened the real serving and MCP source. It caught five factual overgeneralizations the verifier had waved through: "Every read accepts as_of" was wrong because only list, get and the two FHIR routes do, not diff, explain, search or MCP; a caption claiming every response is the stored record unchanged was wrong for FHIR, which projects into an R4 resource, and for diff, which returns a `DiffResponse`; the determinism sentence was overstated; the MCP row was over-compressed; and the `CodeSystem.count` description under `as_of` was imprecise. I corrected all five.

Correction: I corrected the remaining PR #25 determinism overclaim after codex round 2. Codex confirmed four C5 fixes resolved and flagged the determinism sentence as only partially fixed. "The same request yields the same response" was still too strong for the Postgres search path, which orders by pgvector distance with no secondary tie-breaker, while only the offline SQLite path breaks ties deterministically. I fixed the sentence in `0f73a97`, from "the interface is therefore deterministic and safe to integrate against: the same request yields the same response, with no randomness and no wall-clock" to "the interface is therefore safe to integrate against: no value on it is computed from randomness or the wall-clock, the record fields it returns are the frozen values verbatim, and every frozen record it exposes carries the canonical-content SHA-256 hash that pins its integrity."

AI suggestion rejected: I rejected a PR #25 code fix as out of docs scope. Codex noted that the MCP `search_tariffs` tool passes a `system` argument to `/api/v1/search`, which exposes only `q` and `limit`, so the filter silently no-ops. That is a latent code bug needing a `services/` change and the determinism-auditor. I flagged it as a follow-up in the PR and report rather than fixing it, and made sure the doc does not claim that filter.

The owner then overturned PR #24's judgment call. PR #24 had kept the official German rubric quotations verbatim and glossed them in English to protect the `tools/cas_check.py` grep anchors that match German rubric strings. The lecturer rule was stricter: no German/English mix anywhere in the PDF. PR #26 removed all German from the PDF-bound docs and retargeted the nine affected `cas_check.py` grep anchors from German to English in lockstep, keeping the structural floor at 63/63. The anchor edits were grep-string-only: IDs, weights, predicate logic and the German display labels were left unchanged, so `tools/cas_baseline.json` stayed untouched. One prompt-to-diff example: the `12.ai` anchor regex `r"Tests der KI-Anteile"` became `r"[Tt]ests of the AI components"` in step with the arc42/13 heading `## Tests of the AI components (Tests der KI-Anteile)` becoming `## Tests of the AI components`. Six anchors were the ones the prompt named; three more (`2.zielwert`, `2.messverfahren`, `12.ai`) were forced by the prompt's "any other German literal grepped against a PDF-bound doc" clause. PR #26 squash-merged as `708b4a9 docs: 100% English thesis (zero German, no mix) + English cas_check anchors (#26)` on the green contract. Full CI was green including the visible determinism-boundary step. No `services/`, no freeze-line, `cas_baseline.json` untouched.

For PR #26 I delegated review to the verifier, Opus in fresh-context diff-vs-spec mode, and the codex-reviewer, gpt-5.5 as the independent second family. Codex ran a real review with 79'505 tokens in session `019ec644`. The verifier returned PASS. Codex returned CHANGES-REQUESTED.

AI suggestion accepted: I corrected two umlaut-free German common nouns codex caught in PR #26: `Arzneimittelliste` in `ai-tools.md` and `fazit-note` in `fazit.md`. Neither carries an umlaut and neither was in my wordlist, and the same-family verifier waved them through because it checked diff-vs-spec rather than crawling for German. I fixed both in `15a1575` as `official BAG list sample` and `conclusion-note drafting`; neither backed an anchor, so the floor stayed 63/63.

AI suggestion accepted: I accepted the recurring German-scan lesson from PR #26: the umlaut-plus-wordlist scan is not sufficient, and the cross-family reviewer that reads the whole corpus catches umlaut-free nouns.

AI suggestion rejected: I rejected changes to source names and paths in PR #26. I kept `Pos.-Nr.` as the EAL source's own column field name, glossed "position number", and kept the repo paths `vault/fazit-notes` and `method/fazit.md` as data/path names per the owner's exception. I verified at the deliverable level that every single umlaut or accent token in the rendered PDF was a proper noun or data value: `Spezialitätenliste`, `Ünlü`, `hématocrite`, `Diätetische Lebensmittel`, and the literal `ä` escape-artifact evidence.

I updated the public-facing documentation after the PDF-bound language pass. `e1e0ef2 docs(site): brand+logo, clean nav, drop em-dashes/PDF link; pro README; AI-build showcase` covered the site and README cleanup. `22043f1 chore: untrack internal CAS grade-audit (course-internal, not public)` removed the internal grade audit from tracking. `bae6329 docs: final-fixes prompt` recorded the final-fixes prompt. `33387ea vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]` recorded the curated journal state.

I then ran the independent senior audit's final correctness pass. PR #27 squash-merged as `6403a04 docs: CAS final correctness fixes (UC-05, figure em-dashes, dates, ADR-018, visibility, CI pins) (#27)` on the green contract. Seven fixes: the UC-05 diagram-vs-text contradiction, em-dashes stripped from embedded figures, the 22 Jun 2026 Fable-to-Opus date reworded to future tense, the missing ADR-018 row added to the arc42/09 register, repo-visibility wording reconciled to "public and anonymously accessible", and the floating `trivy-action@master` / `sbom-action@v0` CI security actions pinned to commit SHAs. The UC-05 diagram was set to live to match the catalogue and acceptance tables. Scope stayed in `docs/` and `.github/`: no `services/`, no freeze-line. `cas_check` held 63/63, MkDocs `--strict` exited 0, the em-dash grep was 0 across all diagram sources and figures and the 20-file PDF-bound markdown set, and the PDF was rebuilt at 5.6 MB after clearing the SVG-to-PDF cache so the figures re-rendered.

One PR #27 prompt-to-diff example: in `use-cases.svg` the UC-05 ellipse `stroke:#B45309` became `#11365F`, the italic `«designed»` text element was deleted, the two label lines recoloured `#B45309` to `#000000`, and the legend `(UC-02, UC-05)` became `(UC-02)`. UC-02 was deliberately left `<<designed>>` to match the arc42/01 catalogue.

For PR #27 I delegated review to the verifier, Opus in fresh-context diff-vs-spec mode, and the codex-reviewer, gpt-5.5 as the independent second model family. The verifier returned PASS 8/8. Codex used about 570k tokens at xhigh reasoning and independently confirmed both CI SHA pins map to v0.36.0 and v0.24.0 via the GitHub release pages, and that the hand-edited SVGs are well-formed via `xmllint`. I also ran a five-agent adversarial completeness Workflow before committing: em-dash sweep, UC-05 consistency, date/register/visibility, CI/scope, then synthesis.

AI suggestion accepted: I accepted the PR #27 audit prompt's em-dash finding and widened it after checking the build path. The prompt named three SVGs, but tracing `tools/build_pdf.py` showed it normalizes em-dashes only in markdown, not in embedded SVG figures, so five other PDF-bound SVGs would have shipped visible em-dashes. I fixed all eight figures plus their `.puml`, `.dot`, `.iuml` and `render.sh` sources, so `grep` over `docs/diagrams/` returns zero.

AI suggestion accepted: I accepted the Workflow completeness audit finding that my first M1 fix had not reached `docs/adr/004-freeze-content-hash-lineage.md:17`. That line still called point-in-time and diff queries "not yet implemented", contradicting ADR-008's live status and the corrected diagram. I reworded it to live and cross-linked ADR-008.

AI suggestion rejected: I rejected a PR #27 codex finding as out of scope. Codex gpt-5.5 caught the same stale "ended 22 Jun 2026" past-tense date in four root files outside the task's stated scope: `CLAUDE.md`, `AGENTS.md`, `SETUP.md`, `NEXT_STEPS.md`. Two are owner-curated governance files. Codex's own assessment called it a fast follow-up, not a hard stop. I dispositioned it as out of scope and flagged it for a separately authorized change rather than edit governance files autonomously.

I avoided touching the freeze line in PR #27. I skipped optional task 7, widening the ingestion determinism guard, because editing `tests/test_determinism_boundary.py` trips the `guard_frozen` hook. I recognized this before contact, so the guard never had to fire.

Build note: `tools/build_pdf.py` `convert_svg()` caches SVG-to-PDF by existence only with `if not out.exists()`. After editing a diagram, I had to remove `build/pdf/img/<name>.pdf` before rebuilding, otherwise the stale render would ship. I verified the fix by checking that the rebuilt PDF text had 0 `fhir_parser` hits.

Build note: `§` renders as `ğ` under the Type1 helvet font on body pages because the `§` character is not in `build_pdf.py`'s `NORMALIZE` map. This was pre-existing and unrelated to the translation, so I left it for a separately scoped change. I also deferred `/graphify --update` for the now-English doc nodes because the DoD item needs an attended run given the known hook-runaway risk.

## Today's commits

- `1d75fdd vault: codex second opinion on grade estimate [skip ci]`
- `5c41712 chore(vault): journal + index (auto, session end) [skip ci]`
- `b564c17 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `810a4fa chore(vault): journal + index (auto, session end) [skip ci]`
- `6403a04 docs: CAS final correctness fixes (UC-05, figure em-dashes, dates, ADR-018, visibility, CI pins) (#27)`
- `d83121a vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `bae6329 docs: final-fixes prompt`
- `e1e0ef2 docs(site): brand+logo, clean nav, drop em-dashes/PDF link; pro README; AI-build showcase`
- `22043f1 chore: untrack internal CAS grade-audit (course-internal, not public)`
- `33387ea vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `0cdfda5 vault: codex second opinion on grade estimate [skip ci]`
- `5f179f5 chore(vault): journal + index (auto, session end) [skip ci]`
- `a261698 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `75471e3 chore(vault): journal + index (auto, session end) [skip ci]`
- `708b4a9 docs: 100% English thesis (zero German, no mix) + English cas_check anchors (#26)`
- `3a19d00 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `77bbedc docs: no-mix english fix prompt`
- `b978a67 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `7117204 vault: codex second opinion on grade estimate [skip ci]`
- `ef52cab chore(vault): journal + index (auto, session end) [skip ci]`
- `2285856 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `73d3be1 chore(vault): journal + index (auto, session end) [skip ci]`
- `cebed36 docs(arc42): add interface-contract view to the interaction perspective (C5) (#25)`
- `6d6ff2e vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `e38b082 chore(vault): journal + index (auto, session end) [skip ci]`
- `c4b66f9 docs: make the CAS thesis fully English (#24)`
- `a8b4383 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `497e31b docs: fully-english + c5 interface prompts`
- `0b5619c vault: journal 2026-06-14`
- `5273736 chore(vault): journal + index (auto, session end) [skip ci]`
- `1a9317a vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `e6deb5d vault: codex second opinion on grade estimate [skip ci]`
- `7d0764a chore(vault): journal + index (auto, session end) [skip ci]`
- `3143c39 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `5cb8cb1 chore(vault): journal + index (auto, session end) [skip ci]`
- `163eda7 docs(cas): fix three report-vs-repo contradictions (console tests, fhir_parser, Quarkus env) (#23)`
- `98678d4 vault: curate journal 2026-06-13 via gpt-5.5 [skip ci]`
- `8c2d551 docs: cas correctness-fix prompt`
- `2dfb719 chore(cockpit): design package under docs/cockpit, excluded from build (deferred post-submission, extract per ADR-022)`
- `926cdaf Update gitignore`
- `c0b56e4 chore(vault): untrack auto-generated 00-index.md (brain_sync rebuilds it locally)`
- `cbfa108 vault: curate journal 2026-06-13 via gpt-5.5 [skip ci]`
- `72007b4 vault: codex second opinion on grade estimate [skip ci]`

<!-- session ended 12:06 · branch: main · last commit: 163eda7 docs(cas): fix three report-vs-repo contradictions (console tests, fhir_parser, Quarkus env) (#23) -->

<!-- session ended 12:15 · branch: main · last commit: 3143c39 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci] -->

<!-- session ended 12:25 · branch: main · last commit: 1a9317a vault: curate journal 2026-06-14 via gpt-5.5 [skip ci] -->

<!-- session ended 13:53 · branch: main · last commit: c4b66f9 docs: make the CAS thesis fully English (#24) -->

<!-- session ended 14:32 · branch: main · last commit: cebed36 docs(arc42): add interface-contract view to the interaction perspective (C5) (#25) -->

<!-- session ended 14:40 · branch: main · last commit: 2285856 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci] -->

<!-- session ended 15:27 · branch: main · last commit: 708b4a9 docs: 100% English thesis (zero German, no mix) + English cas_check anchors (#26) -->

<!-- session ended 15:36 · branch: main · last commit: a261698 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci] -->

<!-- session ended 18:39 · branch: main · last commit: 6403a04 docs: CAS final correctness fixes (UC-05, figure em-dashes, dates, ADR-018, visibility, CI pins) (#27) -->

<!-- session ended 18:48 · branch: main · last commit: b564c17 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci] -->
