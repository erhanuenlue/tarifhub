# 2026-06-14: CAS doc correctness pass, fully-English thesis, and C5 interface-contract view

I fixed three self-contradictions an independent review found between the submission docs and the repo. The change shipped through PR #23, squash-merged as `163eda7 docs(cas): fix three report-vs-repo contradictions (console tests, fhir_parser, Quarkus env) (#23)`. Scope stayed in docs and `.env.example`: no `services/` edits and no freeze-line changes. `cas_check` stayed 63/63, MkDocs `--strict` was clean, and the PDF was rebuilt without em dashes.

I then applied the owner decision from 14 Jun that the CAS thesis must read as a fully English document. PR #24 squash-merged as `c4b66f9 docs: make the CAS thesis fully English (#24)`. Scope stayed in `docs/` and `tools/build_pdf.py`: no `services/`, no freeze-line files, and `tools/cas_check.py` / `tools/cas_baseline.json` untouched. `cas_check` held 63/63 throughout, the German rubric anchors were preserved verbatim and glossed in English by design, MkDocs `--strict` exited 0, full CI was green, the PDF was rebuilt at 100 pages, and the thesis prose had zero em dashes.

Acting on the independent reviewer's criterion-5 finding, I added an explicit interface-contract view to the interaction perspective. PR #25 squash-merged as `cebed36 docs(arc42): add interface-contract view to the interaction perspective (C5) (#25)`. I added arc42/06 section 6.5 for the L1 serving surface, REST, FHIR R4, MCP, as a tested contract with `as_of`, versioning and diff semantics, a pointer to the generated OpenAPI document and `services/serving/tests/test_openapi.py`, and the determinism guarantee stated at the interface level. I committed one Eraser sequence diagram as PNG plus `.eraser` source and repointed the criterion-map C5 interaction row from the use-case diagram to this view. Docs only: no `services/`, no freeze-line, `tools/cas_check.py` and `cas_baseline.json` untouched. `cas_check` held 63/63 with criterion 5 = 4/4, MkDocs `--strict` was clean, em-dash grep was 0, the PDF was rebuilt with the new section and diagram, and full CI was green on the PR and on main including the images job.

Today's commits:
- `73d3be1 chore(vault): journal + index (auto, session end) [skip ci]`
- `cebed36 docs(arc42): add interface-contract view to the interaction perspective (C5) (#25)`
- `6d6ff2e vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `e38b082 chore(vault): journal + index (auto, session end) [skip ci]`
- `c4b66f9 docs: make the CAS thesis fully English (#24)`
- `a8b4383 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `497e31b docs: fully-english + c5 interface prompts`
- `0b5619c vault: journal 2026-06-14`
- `5273736 chore(vault): journal + index (auto, session end) [skip ci]`
- `1a9317a vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `e6deb5d vault: codex second opinion on grade estimate [skip ci]`
- `7d0764a chore(vault): journal + index (auto, session end) [skip ci]`
- `3143c39 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci]`
- `5cb8cb1 chore(vault): journal + index (auto, session end) [skip ci]`
- `163eda7 docs(cas): fix three report-vs-repo contradictions (console tests, fhir_parser, Quarkus env) (#23)`
- `98678d4 vault: curate journal 2026-06-13 via gpt-5.5 [skip ci]`
- `8c2d551 docs: cas correctness-fix prompt`
- `2dfb719 chore(cockpit): design package under docs/cockpit, excluded from build (deferred post-submission, extract per ADR-022)`
- `926cdaf Update gitignore`
- `c0b56e4 chore(vault): untrack auto-generated 00-index.md (brain_sync rebuilds it locally)`
- `cbfa108 vault: curate journal 2026-06-13 via gpt-5.5 [skip ci]`
- `72007b4 vault: codex second opinion on grade estimate [skip ci]`

## Delegated AI work

I delegated review to the verifier, Opus in fresh-context diff-vs-spec mode, and to the codex-reviewer, gpt-5.5 as the independent second model family, on every PR round.

For the fully-English thesis work, I used a 26-agent Workflow fan-out: one Opus worker per doc, translate then fresh-context verify. The orchestrator, Opus 4.8, kept the LaTeX change limited to redefining `\maketitle` and `\titlehead` in the build preamble so the five cover literals hardcoded in the official `ffhsthesis.cls` render in English: "im Studiengang", "von", "Eingereicht bei:", the running-head "Studiengang", and the "Referent" label. I did not edit the class file.

For the C5 interface-contract view, I delegated review to the verifier, Opus in fresh-context diff-vs-spec mode, and the codex-reviewer, gpt-5.5 as the independent second family. The verifier returned PASS. Codex round 1 used 145k tokens. Codex round 2 used 58.8k tokens.

## Accepted, corrected and rejected AI suggestions

Accepted: the verifier caught the green overclaim in my first pass. The CI `console` job actually runs `1 passed / 1 skipped` because the second Playwright spec is `test.skip(!CAPTURE,...)`, a screenshot helper. I corrected the documentation instead of claiming two green Playwright end-to-end tests.

Accepted: codex gpt-5.5 independently caught the same class of errors and more: the phantom `parsers/fhir_parser.py` in the C4 component diagram embedded in arc42 §5, the same phantom in UC-08, the `typecheck` overclaim because CI runs `npm ci`, lint, build and test but never `npm run typecheck`, the parser-pairing error because `_PARSERS` maps `bag_eal` to `bag_eal`, not to `bag_eal` plus `xlsx_parser`, and that "green on latest main" was unbacked.

Accepted: round-2 codex caught about 10 em dashes in the regenerated diagram SVG. I removed them to keep the owner law for documentation, report and PDF-bound text.

Corrected: one of my own correctness claims was wrong. I wrote that until 2026-06-14 no test script existed. `git show 265b2e3:apps/tarifguard/package.json` proved the `test` script shipped with the console on 2026-06-13 in PR #16. I reframed the issue as stale docs from day one.

Accepted: for criterion 14, I replaced an unquoted green claim with real pipeline output from `gh run view --log` in arc42/13: `Tests 18 passed (18)` and `Running 2 tests / 1 skipped / 1 passed (17.2s)`. I interpreted it as 18 Vitest tests passing, one e2e smoke passing, and one CAPTURE-gated screenshot spec skipped. This is quoted and interpreted pipeline output, not a screenshot as evidence.

Rejected for the PR #23 task scope: codex flagged real P1s in `services/`, including `:8080` defaults in mcp/intelligence and a `fhir_parser` reference in ingestion/README, plus a pre-existing review-endpoint contradiction. I did not fix them because the prompt forbade `services/` edits and they were a fourth contradiction beyond the three named. I flagged them in the PR and report for a separately authorized task, which would also need determinism-auditor.

Corrected: my own heuristic German scan for PR #24, umlauts plus a function-word list, missed `docs/method/ai-se-framework.md` and `decision-matrix.md`, because their German words such as "Generierung", "Recherche", and "Fazit" carried no umlauts and were not in my wordlist. The verifier PASSed because it only checked the diffed files, but codex gpt-5.5 crawled the whole repo and flagged them. It also flagged four wording nits: "equally valid to" to "as", "released data" to "frozen data", the phase list, and "List of Resources" to "List of Aids and Tools". I fixed these in `437764b`; codex re-review returned LGTM.

Accepted: the per-file verify stage of the translation workflow flagged glossed-anchor leftovers such as "(Problemstellung)", "(Fehlerbehandlung)", the connective "je", and "nie an die KI delegiert". The orchestrator removed them so that only the `cas_check` rubric anchors remain German.

Rejected as a judgment call: I did not let the AI fully translate the official German rubric quotations. The criterion-8/12/18 quotes, for example „...als Fazit reflektiert", „gewählten Frameworks", and „Tests der KI-Anteile", are kept verbatim as citations and glossed in English, because a thesis quotes its grading rubric in the original.

Accepted: the `\maketitle` redefinition produced a cover that now renders "Submitted to:", "Advisor:", "by", "in the study programme CAS AI-Assisted Software Engineering", and "Switzerland, 2026", all previously German, with `ffhsthesis.cls` untouched. I verified this by reading the rendered PDF pages.

Accepted for PR #25: codex round 1 opened the real serving and MCP source and caught five factual overgeneralizations the verifier had waved through: "Every read accepts as_of" was wrong because only list, get and the two FHIR routes do, not diff, explain, search or MCP; a caption claiming every response is the stored record unchanged was wrong for FHIR, which projects into an R4 resource, and for diff, which returns a `DiffResponse`; the determinism sentence was overstated; the MCP row was over-compressed; and the `CodeSystem.count` description under `as_of` was imprecise. I corrected all five.

Corrected for PR #25: codex round 2 confirmed four C5 fixes resolved and flagged the determinism sentence as only partially fixed. "The same request yields the same response" was still too strong for the Postgres search path, which orders by pgvector distance with no secondary tie-breaker, while only the offline SQLite path breaks ties deterministically. I fixed the determinism overclaim in `0f73a97` by scoping the sentence to the value contract, from "the interface is therefore deterministic and safe to integrate against: the same request yields the same response, with no randomness and no wall-clock" to "the interface is therefore safe to integrate against: no value on it is computed from randomness or the wall-clock, the record fields it returns are the frozen values verbatim, and every frozen record it exposes carries the canonical-content SHA-256 hash that pins its integrity."

Rejected for PR #25 scope: codex noted that the MCP `search_tariffs` tool passes a `system` argument to `/api/v1/search`, which exposes only `q` and `limit`, so the filter silently no-ops. That is a latent code bug needing a `services/` change and the determinism-auditor, out of scope for this docs-only task. I flagged it as a follow-up in the PR and report rather than fixing it, and made sure the doc does not claim that filter.

## Build notes

`tools/build_pdf.py` `convert_svg()` caches SVG-to-PDF by existence only with `if not out.exists()`. After editing a diagram, I had to remove `build/pdf/img/<name>.pdf` before rebuilding, otherwise the stale render would ship. I verified the fix by checking that the rebuilt PDF text had 0 `fhir_parser` hits.

`§` renders as `ğ` under the Type1 helvet font on body pages because the `§` character is not in `build_pdf.py`'s `NORMALIZE` map. This was pre-existing and unrelated to the translation, so I left it for a separately scoped change. I also deferred `/graphify --update` for the now-English doc nodes because the DoD item needs an attended run given the known hook-runaway risk.

<!-- session ended 12:06 · branch: main · last commit: 163eda7 docs(cas): fix three report-vs-repo contradictions (console tests, fhir_parser, Quarkus env) (#23) -->

<!-- session ended 12:15 · branch: main · last commit: 3143c39 vault: curate journal 2026-06-14 via gpt-5.5 [skip ci] -->

<!-- session ended 12:25 · branch: main · last commit: 1a9317a vault: curate journal 2026-06-14 via gpt-5.5 [skip ci] -->

<!-- session ended 13:53 · branch: main · last commit: c4b66f9 docs: make the CAS thesis fully English (#24) -->

<!-- session ended 14:32 · branch: main · last commit: cebed36 docs(arc42): add interface-contract view to the interaction perspective (C5) (#25) -->

