# 2026-06-14 — CAS doc correctness pass (3 report-vs-repo contradictions)

Fixed three self-contradictions an independent review found between the submission docs and the repo, shipped via the 9-phase pipeline (PR #23, squash-merged `163eda7`, CI fully green). Scope held to docs + `.env.example`; no `services/`, no freeze line. cas_check 63/63 throughout; mkdocs `--strict` clean; PDF rebuilt em-dash-free.

**Delegated.** verifier (Opus, fresh-context diff-vs-spec) and codex-reviewer (gpt-5.5, independent second family) on every PR round. Both earned their keep.

**Where AI got it wrong, and what caught it.** My own first pass fixed the three named files but, while doing so, (1) wrote "2 Playwright end-to-end tests, green" and kept a "typechecks the console" claim, and (2) left the *same* phantom `parsers/fhir_parser.py` in the C4 component **diagram** (embedded in arc42 §5 as graded evidence) and in UC-08.
- verifier caught the green overclaim: the CI `console` job actually runs `1 passed / 1 skipped` because the second Playwright spec is `test.skip(!CAPTURE,...)`, a screenshot helper.
- codex (gpt-5.5) independently caught the diagram phantom, the `typecheck` overclaim (CI runs npm ci/lint/build/test, never `npm run typecheck`), the parser-pairing error (`_PARSERS` maps `bag_eal`->`bag_eal`, not `bag_eal`+`xlsx_parser`), and that "green on latest main" was unbacked. Round-2 codex caught ~10 em-dashes riding into the regenerated diagram SVG (no-em-dash owner law).
- One of my own correctness claims was itself wrong: I wrote "until 2026-06-14 no test script existed". `git show 265b2e3:apps/tarifguard/package.json` proved the `test` script shipped with the console on 2026-06-13 (PR #16); the docs were simply stale from day one. Reframed accordingly.

**Prompt -> diff example (crit 14).** Instead of asserting a green I had not quoted, I pulled the real CI console-job log (`gh run view --log`) and quoted it in arc42/13: `Tests 18 passed (18)` then `Running 2 tests / 1 skipped / 1 passed (17.2s)`, with the interpretation (18 Vitest tests pass; 1 e2e smoke + 1 CAPTURE-gated screenshot spec skipped). Quoted-and-interpreted output, not a screenshot.

**Dispositioned, not delegated.** codex flagged real P1s in `services/` (`:8080` defaults in mcp/intelligence; a `fhir_parser` reference in ingestion/README) and a pre-existing review-endpoint contradiction. I did not fix them: the prompt forbids `services/` edits and they are a 4th contradiction beyond the three named. Flagged in the PR + report for a separately-authorized task (which would also need determinism-auditor). Scope discipline over a tempting one-line sweep.

**Gotcha.** `tools/build_pdf.py` `convert_svg()` caches SVG->PDF by existence only (`if not out.exists()`), so editing a diagram needs `rm build/pdf/img/<name>.pdf` before rebuild or the stale render ships. Verified the fix: 0 `fhir_parser` in the rebuilt PDF text.

