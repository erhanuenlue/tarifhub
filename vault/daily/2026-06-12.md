# 2026-06-12 — AI workflow journal

> Draft created by SessionEnd hook — CURATE before counting it as evidence.
> 3–6 honest lines. Contemporaneous or worthless.

- Delegated: ePL/SL FHIR-R5 source + read-API parity + FR ranking fix as 3 parallel `implementer` (Opus 4.8) branches in isolated worktrees; live SL ingest + FR eval to `e2e-tester` (Sonnet); per-PR review waves: `verifier`, `determinism-auditor` (Sonnet), `security-reviewer` (Opus), `codex-reviewer` (gpt-5.5). Fable 5 kept plan, orchestration, merge-gate reads. PRs #5/#6/#7.
- AI got wrong → caught by: (1) task spec assumed SL has point-carrying entries — the full 93 MB export has zero tax points (money-only) → caught by recon data inspection; (2) recon brief misread the IG: no `/CHIDMP/` in fullUrl, three RAs per package not a pair → caught by implementer against real bundles; (3) recon called Block-0 query embeddings "raw" — they were passage-prefixed → caught by fresh-context verifier before it entered graded docs (codex then proposed a wrong fix from the same confusion — rejected); (4) e2e-tester diagnosed re-run re-versioning as a TOCTOU race in `repo.add()` → refuted at the merge gate (audit timestamps prove sequential runs; `UNIQUE(record_hash)` + zero duplicate hashes); real cause: live `ai_map` fills vary run-to-run → 55 re-versions (board #14); (5) codex P1 "a billing value must exist at freeze" was an invented invariant → rejected against the gate-01 design + EAL precedent, while its coding[0] P2 was real and got fixed.
- One concrete prompt → diff example: "live fills are not byte-stable across runs — say exactly that" → ADR-015 consequences sentence + §10 reproducibility note with the measured version chain (GTIN 4003053091007: "Diätetische Lebensmittel" → "Spezialnahrung / Stoffwechseldiät" → "Spezialnahrung bei Stoffwechselstörungen").
- Decision made (→ ADR?): ADR-015 (SL mapping: one record per reimbursed package, GTIN key, retail price as `price_chf`, money-only). FR `query:` prefix ships on recall evidence (recall@5 .833→.917) despite the MRR@5 dip — trade-off documented, not hidden. Parked for Erhan: #13 Decimal-scale contract, #14 AI-gap idempotency.

Ship extract (criterion 15): three-PR pipeline, 17 agent runs across 4 models (Fable orchestration, Opus implement/security, Sonnet verify/audit/e2e, gpt-5.5 codex). Worktree-collision dispatch failures (branch already checked out) recovered via SendMessage resumes. Every reviewer finding dispositioned in the PR bodies; live evidence: 10,299 SL records frozen at 1.08 % review rate, 109 fail-closed; FR eval before/after tables in docs/evidence/.

**Second ship (same day) — hash-integrity pair (PR #8, merged d027a1d):**

- Delegated: one `implementer` (Opus 4.8) built both owner decisions (#13 Decimal scale contract → ADR-016; #14 AI fill-reuse + `--refill` → ADR-005 addendum) on one branch; `e2e-tester` (Sonnet) ran the live two-leg proof; verifier + determinism-auditor + codex (gpt-5.5) reviewed — zero defects, first empty fix-cycle of the project.
- Proof design worth keeping: the reuse leg ran with a deliberately INVALID API key — frozen=0 (10,299/10,299 skipped) is then airtight evidence of zero AI reliance, no log forensics needed. Bonus measured nuance: under `--refill` only 20 of ~47 gap records re-versioned; the other fresh fills came back byte-identical and deduped by hash.
- AI got wrong → caught by: codex CLI failed to emit a formal report in 2 of 6 sessions this block (one plugin error, one silent termination); the bridge agent labeled Codex-probed evidence vs in-family fallback honestly — multi-tool review needs that discipline. Two earlier worktree dispatch failures traced to git's one-checkout-per-branch rule (fixed via SendMessage resumes; rule now in auto-memory).
- Decision (→ ADR): ADR-016 frames the scale contract as hash integrity (stored bytes ≡ hashed bytes on every engine); ADR-005 addendum: fill-reuse provenance lives in audit detail, never record metadata (byte-stability trade-off, deliberate).

**Tooling debt closed — graphify hooks (board #15, PR #9):**

- The knowledge graph kept self-destructing: upstream `graphify.watch._rebuild_code` (inside BOTH generated git hooks) full-walks the tree (~77k vendored nodes twice today) and rebuilds code-only (every doc/image node dropped per commit). Layered diagnosis paid off twice over: a `.graphifyignore` already existed but was (a) gitignored and (b) never consulted by the hook path — only `detect()` reads it; and the post-checkout hook ran the rebuild synchronously, which I only found because my own background `git checkout -b` chain hung on it for 20 minutes.
- Fix at the right layer (owner-directed): `.graphifyignore` tracked as single source of truth; tracked incremental hooks consume it via the library's own loader; async wrapper untouched; `install.sh` + QUICKSTART note make it survive any future `graphify hook install`.
- Proof as specified: two consecutive real commits through the live async chain — node count stable at 1,368, doc 315/image 113 preserved, zero vendored, `verify_graph.py --json` as the permanent health check.
- Fazit note: a hook that silently rewrites derived state is invisible until something measures it — the graph audit (criterion-16 tooling) was itself the detector.

**Main images job unbroken (PR #10):** the missing kassenflow/meldepilot lockfiles were a two-layer problem — `npm ci` hard-fails without them, and once generated they made the stubs' dependency trees visible to Trivy for the first time: next@14.2.5 carried a CRITICAL (CVE-2025-29927) + 9 HIGH that had been sitting in package.json unscanned. Fixed by aligning both stubs to tarifguard's proven-clean set (next 15.5) instead of suppressing the gate; both images verified building locally before push; post-merge main run fully green incl. images. Lesson for the Fazit: a lockfile isn't just reproducibility — it's what makes the dependency tree *auditable*; the security gate could only bite once the pin existed.

**Node-24 readiness chore (PR #11):** FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 in ci.yml + docs.yml ahead of GitHub's 16 June force-switch; six action majors bumped against verified release tags per Erhan's "verify, don't guess" — and even that wasn't enough: astral-sh publishes no floating v8 alias for setup-uv (only exact v8.x tags; v6/v7 have aliases), so the first CI run failed in 2s on tag resolution and the fix is an exact `@v8.2.0` pin. Both post-merge main workflows green under Node-24 forcing, images included.

↩ [[00-index]] · [[fazit-notes]]

<!-- session ended 00:19 · branch: main · last commit: b5a0017 feat(shipboard): vault graph — zoom/pan, frozen-after-settle layout, reliable click-to-read -->

<!-- session ended 00:23 · branch: main · last commit: 38818e1 chore(vault): journal + index (auto, session end) [skip ci] -->

<!-- session ended 12:15 · branch: main · last commit: 0a5495b feat(shipboard): GitHub tab — live runs, PRs, branches, git ops, run/PR inspectors -->

<!-- session ended 12:16 · branch: main · last commit: 6b2ae86 chore(vault): journal + index (auto, session end) [skip ci] -->

<!-- session ended 12:34 · branch: main · last commit: e4bda77 fix(shipboard): inspect() drops ids for ghpr/ghrun/note — route all id-bearing types -->

**L1 completeness ship (PR #12, merged 257c0e0) — TarifCore serves what the docs promise, TarifMCP proven:**

- Delegated: 4 `implementer` (Opus 4.8) tasks — serving core (as_of/diff/search-fallback/explain), FHIR R4 adapter, MCP ASGITransport integration tests, arc42 acceptance-criteria + test-strategy artifacts; `e2e-tester` (Sonnet) 24-item runtime smoke vs real Postgres + live MCP over streamable-http; review wave `verifier` + `determinism-auditor` (Sonnet) + `security-reviewer` (Opus) + `codex-reviewer` (Codex CLI). Fable 5 kept: gate-01 plan (5 baked decisions Erhan approved), orchestration, merge-gate read, inline glue (OpenAPI meta-test, coverage wiring, ADR-017, doc sync).
- AI got wrong → caught by: (1) docs implementer wrote the *superseded* 501-on-SQLite search contract into the acceptance table + strategy page and an AI-labelled explain into UC-09 — both contradicted the gate-01-approved design → caught by orchestrator review of the agent report before commit; (2) my own dispatch spec claimed SearchHit carries a `similarity` field — the implementer corrected against real code (`{rank, record}`); (3) serving-core agent mis-reported the pgvector test as "already existed" (it created it); (4) verifier caught two stale docstrings + future-tense acceptance rows that would have gone stale at merge; (5) codex review mode again emitted diagnostics without a synthesized verdict — bridge labelled its synthesis honestly; codex's one real suggestion (ADR-003 field-set drift guard) became `test_model_contract.py`.
- Prompt → diff: "explain must be deterministic, record-grounded, no LLM on the serve path — label it" → `explain.py:build_explanation` with `[deterministic]` prefix + MCP integration test asserting `startswith("[deterministic]")` against the real app.
- Decisions (→ ADR-017): SQLite search ranks via in-process cosine fallback (offline-testable, demoable) + `/api/v1/explain` live as deterministic endpoint; security P2 (unbounded fallback scan) fixed same-session with a deterministic candidate cap, objecting reviewer re-check CONFIRMED.
- Ops note: the graphify post-commit hook spun ~23 CPU-min at 100% on a small commit batch (killed; stale index.lock cleared) — board-#15 fix has a remaining pathology, cache dir at 14k entries. Erhan committed `feat(shipboard)` tab-7 onto the ship branch mid-pipeline; rode along in the squash with attribution preserved.

Ship extract (criterion 15): one PR through all 9 phases; 11 agent runs across 4 model families (Fable plan/orchestrate/gate, Opus implement+security, Sonnet determinism+e2e, Codex second family); exploration via a 5-reader parallel workflow (~300k tokens distilled before planning). CI green with boundary tests as a named step and the new coverage step printing serving 95 % / mcp 91 % / ingestion 89 %; 24/24 runtime checks vs real Postgres; auto-merged under the phase-09 green-contract (all four conditions verified, owner decision 2026-06-11).

**Rubric-refresh docs chore (PR #13) — criterion 8+17 wording aligned:**

- Delegated: nothing — 4-file docs diff below dispatch threshold, Fable implemented inline; `verifier` (fresh context) was the single reviewer per approved routing (no `services/` → no determinism-auditor).
- AI got wrong → caught by: Fable put a period inside the "verbatim" criterion-8 quote, so arc42 §8 and criterion-map disagreed byte-for-byte → caught by verifier (hexdump comparison `2e 22` vs `22 20 28`); one-character fix, gate re-run green. Also: the task list itself misremembered a vault crit-8 row that doesn't exist → caught by pre-plan grep, flagged as no-op at gate 01 instead of inventing a row.
- Prompt → diff: "arc42 §8 opens by quoting the new criterion-8 wording verbatim, framed as a direct claim" → blockquote + four-concept claim paragraph with code pointers, plus owner-approved `### Fehlerbehandlung` section (the only criterion-named concept that lacked one).
- Decision: criterion-17 ellipsis preserved in the map — the full middle text isn't in this workspace (`../../02_CAS/` gone); honest elision beats invented wording.
- Post-merge `/graphify --update` turned into a full semantic re-extract (manifest + cache found empty — fallout of yesterday's cache cleanup): 36 parallel subagents (9 text on Fable, 27 image on Sonnet) over 242 files; merged graph 1,805 nodes / 4,045 edges / 73 labelled communities, `verify_graph.py` healthy after re-pointing 2 nodes whose source_file was a runtime artifact; manifest now saved over the full corpus + 225 files cached, so future updates are incremental again.

<!-- session ended 15:24 · branch: main · last commit: d38f9c1 docs(prompts): grading anchors — interpreted-evidence standard (06), veto-Fazit + Eigenständigkeitserklärung (07) -->

**Grading-anchor tightenings (PR #14) — six artifacts written to the official anchors' top tier:**

- Delegated: 4 parallel `implementer` (Opus 4.8) — §1/§3 Kernfunktionen+context, §10 NfA table+cross-refs, §13 „Tests der KI-Anteile", LEARNINGS.md register; `verifier` (fresh context, framed as the grader) sole reviewer per approved routing. Fable 5: gate-01 plan, orchestration, merge-gate, inline fix glue. Mid-session find: Erhan's gitignored official-rubric working copy (`docs/cas/`) — plan validated against the real anchors post-hoc, mapping held 1:1 (criteria 1/3/2/11/12/9).
- AI got wrong → caught by: (1) verifier's P1 claimed the intelligence boundary test "runs in no workflow" — its literal grep missed ci.yml's `services/*/` glob loop; orchestrator re-read of ci.yml corrected the premise, the narrower true defect (named `-v` step is ingestion+serving only) was fixed; verifier conceded and re-verified empirically (suite exit 0). (2) Fable's `exclude_docs` first attempt put a YAML comment inside the literal block — pattern matched nothing; caught by post-build `ls site/cas`, not by the green `--strict` build. (3) implementer D garbled "embedded search queries" into "evidence queries" — caught at orchestrator read.
- Rejected with reasoning: verifier P2 demanding ADR refs for NfA-4/5/6 — honest "—" beats invented references (anchor asks "Bezug erkennbar", satisfied by NfA-1–3); disposition accepted on re-check.
- Prompt → diff: "no invented numbers — Measured cells only from existing evidence" → NfA-4 reads "not yet measured — method defined" instead of a fabricated p95.
- LEARNINGS.md created (criterion 9): 14 entries, every one with PR+SHA ref and ✓/±/✗ disposition, all traced to this journal. Auto-merged under the phase-09 green-contract (CI green incl. visible boundary step, findings dispositioned, no frozen paths, tree clean apart from hook-owned vault drafts).

**§1 Vision + cas-anchors ratchet (PR #15):**

- Delegated: one `implementer` (Opus 4.8) for both chores — §1 Problemstellung/Vision written to the criterion-3 anchor (Zielgruppe·Bedürfnis·Abgrenzung named substantively, KI-Nutzen nested under Vision) + the `cas-anchors` CI job (ratchet gate, `cas-status` artifact with if:always). `verifier` sole reviewer: PASS A–E, no P1/P2.
- Checker honesty note (verifier's adversarial leg): the structural floor's criterion-3 regexes are hollow-passable (`has(A01, "Vision")` matches anything) — the floor proves presence, the grade-auditor judges prose. Both P3s dispositioned: REST/FHIR wording aligned to §3; the running shipboard rewrites the local baseline timestamp (environment, committed state unaffected).
- Fable's own slip: `git commit --amend` aimed at the docs commit landed the P3 fix in the ci commit (HEAD) — caught on the post-amend log read, left as-is since the PR squash-merges; flagged here for honesty.
- Evidence: cas_check 51/51, criterion 3 → 3/3, zero regressions; PR CI green with cas-anchors proving itself (7 s) and the cas-status artifact (2.7 KB) uploaded next to the SBOM.

<!-- session ended 17:35 · branch: main · last commit: 267afc2 docs+ci: §1 Problemstellung & Vision (criterion 3 → 3/3) + cas-anchors ratchet job (#15) -->

