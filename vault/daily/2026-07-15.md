# 2026-07-15

Erhan asked me to audit and rewrite `CLAUDE.md` and `AGENTS.md` so a fresh Opus 4.8 or Codex session could operate from the files alone. I had the orchestrator fan out seven audit agents, which produced 93 findings across commands, freeze machinery, environment configuration and private-memory landmines. I also used three fresh-context verifiers instead of relying on the orchestrator's own read.

I corrected two AI errors caught by the review net. The first rewrite claimed that low-confidence records skip freezing. A fact-check verifier corrected this against `pipeline.py`, where the sequence is flag-then-freeze. I also corrected an AI-proposed MkDocs installation command that omitted the enumerate-headings plugin.

I accepted and addressed all ten issues that Codex identified in PR #49. This was the first PR reviewed through the new gpt-5.6-sol seat recorded in ADR-020. The findings included three reproducible bypass paths in `.claude/hooks/guard_frozen.sh`. I fixed the findings and re-verified the hook with a 12-case simulation.

Erhan's prompt, `"2. fix it"`, referred to the guard hook. I translated it into normalized paths, anchored regexes, basename-anywhere coverage for both boundary tests and `NNN_*.sql` naming enforcement in `.claude/hooks/guard_frozen.sh`. The change landed in squash `0200831`.

I recorded the following owner decisions: product-build mode applies after the 3 July submission, git is autonomous on all paths, vault evidence remains append-only, the Codex seat uses gpt-5.6-sol through ADR-020, and worker frontmatters are pinned to exact model IDs.

Today's commits:

- `0200831` Instruction-file overhaul: fresh-model self-sufficiency, guard_frozen fix, ADR-020 Codex seat (#49)
- `1010dde` chore(vault): journal + index (auto, session end) [skip ci]

<!-- session ended 21:45 · branch: main · last commit: 0200831 Instruction-file overhaul: fresh-model self-sufficiency, guard_frozen fix, ADR-020 Codex seat (#49) -->
