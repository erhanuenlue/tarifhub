# 2026-07-15

- Erhan asked for an audit and rewrite of the instruction files (CLAUDE.md, AGENTS.md) so a fresh Opus 4.8 or Codex session could operate from the files alone. The orchestrator fanned out 7 audit agents (93 findings across commands, freeze machinery, env, and private-memory landmines) plus 3 fresh-context verifiers instead of trusting its own read.
- AI got it wrong twice and the review net caught both: the first rewrite claimed low-confidence records skip freezing (the fact-check verifier corrected it against pipeline.py: flag-then-freeze) and prescribed an mkdocs install line missing the enumerate-headings plugin. Codex (gpt-5.6-sol, first PR on the new seat, ADR-020) then found 10 real issues in PR #49, including three reproducible guard_frozen bypass paths; all were fixed and re-verified with a 12-case hook simulation.
- Prompt-to-diff example: Erhan's "2. fix it" (the guard hook) became normalized paths, anchored regexes, basename-anywhere coverage for both boundary tests, and NNN_*.sql naming enforcement in .claude/hooks/guard_frozen.sh; landed in squash 0200831.
- Owner decisions recorded: product-build mode after the 3 July submission (autonomous git on all paths, vault evidence stays append-only), Codex seat switched to gpt-5.6-sol (ADR-020), worker frontmatters pinned to exact model ids.

