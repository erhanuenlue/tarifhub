# 2026-06-11 — Block 0: foundation honest (reconciliation + live ai_map)

- **Delegated:** 5 parallel audit agents (recon), 3 implementers — serving port Java→FastAPI (Opus 4.8), live `ai_map` TDD (Opus 4.8), docs/ADR sweep — then 4 reviewers (verifier, determinism-auditor, security-reviewer Opus, **Codex as independent second model family**). I (Fable 5) did plan, orchestration, merge-gate, and the small glue (mcp/docstring port fixes, CI repair, error-path fix).
- **What AI got wrong, and what caught it:** (1) my `ai_map` error path wrote `ai_status` into metadata → different `record_hash` than the no-key fallback, breaking re-ingest idempotency under transient API outages — **caught by Codex review**, not by the 28 green tests; (2) serving `_row_to_record` called `json.loads()` on Postgres JSONB (already a dict) → every Postgres response would 500 — **caught by Codex**; SQLite-only tests were blind to it; (3) the pre-existing CI lint step could never run the determinism evidence (`uv sync` doesn't install ruff) — **caught by the verifier** re-running CI's exact commands.
- **Hook intervention:** `guard_frozen` blocked creating a second file basenamed `test_determinism_boundary.py` (matches by basename); serving's boundary test is `test_serving_boundary.py`, same AST assertions — guard honored, not bypassed.
- **Prompt→diff example:** prompt line "FILL-ONLY: set designation.fr/it/category ONLY where the current value is None" → `tariff_mapper.py` merge block; live EAL run then showed exactly that: fr/it filled, existing category untouched, `ai_fields=['designation_fr','designation_it']`, billing fields byte-identical, confidence 0.9→1.0.
- **Spec deviation recorded:** "structured output, temp 0" — the `temperature` param no longer exists on current Claude models (HTTP 400); determinism is architectural (schema-constrained output, fill-only merge, deterministic scorer). ADR-008.
- **E2E evidence (live key, EAL sample):** 5 in / 5 frozen / 0 flagged (rate 0.0); fallback run no-key @ threshold 0.95: 5/5 flagged `requires_review` — routing knob proven end-to-end.

## Session 2 — Block 0 completion (same day)

- **Guard event — owner-authorized one-line type fix:** the first real Postgres pipeline run exposed `int(validation_ok)` in `audit/audit_logger.py` (below the freeze line) — psycopg sends smallint, Postgres rejects it for the boolean column. `guard_frozen` blocked my edit; per protocol I stopped and asked. Erhan authorized exactly one line (`"validation_ok": validation_ok,`), conditions met: isolated commit `fix(audit): …`, frozen-file diff shown pre-commit, guard re-armed and probe-verified (follow-up Edit attempt BLOCKED), Postgres round-trip regression test added outside the floor (True/False/None, opt-in via `TARIFHUB_PG_TEST_URL`, 3 passed live).
- **What AI got wrong / what caught it today:** same int-for-bool bug class in `tariff_repository.py` (outside floor, fixed directly) — caught by the **first real Postgres run**, not by the 51 green SQLite tests; engine parity is now a named risk with a live regression test.
- **Honest finding on the real EAL:** the official AL 2026-01-01 is complete (1279 trilingual rows, 0 gaps) — fill-only `ai_map` correctly contributes nothing (`ai_assisted=0`); a deterministic gap-gate now skips the API call when nothing is fillable. The AI seam was demonstrated on 3 real positions with FR/IT withheld, graded against BAG's own translations: 4 of 6 fills exact-match the official text, 2 differ only stylistically ("1,25-diidrossi-vitamina D" vs "1,25-diidrossivitamina D"; "Troponina, T o I" vs "Troponina (T o I)"); billing fields byte-identical in all cases.

- **Shipped (session 2):** PR #2 — 12 commits through the full review battery (verifier PASS, determinism-auditor CLEAN incl. the authorized freeze-line commit, security 2 minors fixed pre-merge, Codex 2 advisories fixed + 1 adjudicated as documented single-writer constraint). Console verified green locally before re-pointing CI (npm ci needed a generated package-lock.json; no test script yet → --if-present).
- **Scale evidence:** 1279/1279 real EAL records frozen into Postgres+pgvector with e5 embeddings in 70.6s; review rate 0.0%; serving proof incl. cross-lingual search captured in docs/evidence/.

<!-- session ended 18:38 · branch: fix/images-serving-context · last commit: eea44f4 fix(ci): build serving image with repo-root context -->

## Session 3 — Block 0, prompts/02: spec & design artifacts (criteria 1–6 + 8)

- **Delegated:** one 22-agent workflow — 17 writers (5 new PlantUML sources, 8 arc42 chapters + criterion map, 14 register ADRs, legacy annotations), 1 render/build agent (docker plantuml + uvx mkdocs), 4 fresh-context verifiers (criteria, accuracy, honesty, integrity). I (Fable 5) did plan (approved by Erhan), briefs, all fixes, merge-gate read. Docs-only — no code touched.
- **What AI got wrong, and what caught it:** (1) **my own brief** told all writers "explain_crosswalk returns 501" — the code 404s (no serving route exists); the accuracy verifier checked the actual httpx path and caught my orchestrator-level error after it had propagated into 3 files. (2) Two diagram authors **inverted live freeze semantics** (flagged records drawn as "sits unfrozen in the review queue") while the chapter author got it right from the same brief — caught by accuracy+honesty verifiers against `pipeline.py` (freeze is unconditional; flags ride on frozen records). (3) Exploration missed `services/intelligence/` entirely → ch05 claimed "L2 has no code yet"; verifier found the offline-tested tarifiq scaffold. (4) Pre-existing C4 sources presented XML serving and MinIO archival as live — only flagged once we promoted those diagrams to graded evidence.
- **Diagram delta (criterion 15):** AI drafted all 5 new diagram sources in one pass in the house palette; corrections were semantic, not cosmetic — state diagram re-routed (`scored → frozen` always, review queue holds *frozen* flagged versions, expert decision → NEW version), review-loop precondition note rewritten, REST labels JSON-only, MinIO relation marked "(designed, ADR-007)".
- **Prompt→diff example:** brief line "ADR-005 must NOT regress to 'temp 0' — carry legacy 008's architectural-determinism correction" → `docs/adr/005-single-ai-seam.md` lists "Determinism via 'temperature 0'" as a *rejected alternative* and supersedes the AGENTS.md wording explicitly.
- **Decision recorded:** ADR renumbering — v2.1 register materialised as 001–014, repo-era originals preserved verbatim in `adr/legacy/` + mapping README (contemporaneity kept, AGENTS.md numbering now matches the files). ADR-014: arc42-light over Diátaxis / pure C4+ADR / Starlight.
- **Shipped:** PR #4 (3 commits, ~60 files). Known follow-up: ADRs absent from print-site PDF (nav untouched — mkdocs.yml carries another session's uncommitted changes).
