# 2026-06-13, AI workflow journal

- Today's commits: c3b74d4 chore(loop): bypassPermissions for unattended runs; 8fa21d9 chore(vault): journal + index (auto, session end) [skip ci]; d6b0d40 vault: curate journal 2026-06-13 via gpt-5.5 [skip ci]; 543be72 vault: journal + index; fe93494 chore(vault): journal + index (auto, session end) [skip ci]; b84a550 chore: shipboard fixes, loop secret gate, public-readiness; 7c089ff chore(repo): public-readiness hardening (helm dev password, public README, MIT LICENSE) (#17); 25317ad fix(shipboard): session-gated phase rail, hook delegations, stale-active expiry; 265b2e3 feat(console): Block-05 TarifGuard console (master-detail, review form, explain panel) (#16); 15c2c08 chore: board v9, loop runner, quality-before-cost seats, codex doc review; 7d64a8d chore(vault): journal + index (auto, session end) [skip ci]; bfc170b chore(vault): journal + index (auto, session end) [skip ci]; ad3ff67 chore(vault): journal + index (auto, session end) [skip ci].
- I delegated a 5-agent parallel understand phase for the serving contract, review-data source, brand, arc42 target and CI, plus the merge-gate review with verifier, security-reviewer, codex gpt-5.5 and frontend code-reviewer. I rejected the AI workflow suggestion to use parallel implementation worktrees and kept implementation in-orchestrator on Opus 4.8 because the console was one tightly-coupled contract and brand layer.
- I corrected the previous AI scaffold in `lib/api.ts`: it used a camelCase, Java-flavoured contract (`id`, `taxPoints`, POST explain/coding-check) that rendered every value as a dash placeholder against the real snake_case serving API. I caught it by reading both sides of the wire, not by a test.
- I corrected the AI-generated visual layer: the brand was a teal placeholder and the visual law was not implemented. I implemented `.value-certified` and `.ai-content` from `tokens.css`. I also corrected the evidence screenshots after eyeballing the captured PNGs: the Next.js dev-mode badge leaked into them, so I set `devIndicators: false`.
- AI caught AI: codex gpt-5.5 found a Critical that the verifier and security pass under-weighted, the billing-field guard was client-side only, plus a High where the review BFF silently masked a configured upstream failure as success. I accepted the finding and corrected it. Prompt and diff pointer: codex prompt "try to find what the first-pass reviews missed" flagged the client-only billing guard, then the diff added server-side `BILLING_FIELDS` rejection in `app/api/review/route.ts` (commit 0524f9a).
- The owner chose through AskUserQuestion to extend `apps/tarifguard`, not create a new `-demo` directory. I corrected the stale `apps/tarifguard-demo` path note in `AGENTS.md`. The review write path stayed a console-owned mocked BFF pending the ingestion review endpoint under ADR-013 design scope, so I made no new ADR. Shipped as PR #16, CI fully green.
- Public-readiness chore before go-live: I swept the tree and all 142 commits of history for secrets, verified the ignore-list and that the CAS anchor doc is ignored and untracked, hardened the Helm dev postgres password to `changeme-in-production`, rewrote the README as a public front door, removed private `tarifhub-fable5` corpus references and added an MIT LICENSE. I respected `.env` file read protection by verifying `.env.example` indirectly with var-name grep and the secret sweep, not by reading or blind-editing protected files. Shipped as PR #17, CI and gitleaks green. Visibility and Pages stayed with the owner.
- Session end refs kept for traceability: d121353 chore(vault): journal + index (auto, session end) [skip ci]; ad3ff67 chore(vault): journal + index (auto, session end) [skip ci]; bfc170b chore(vault): journal + index (auto, session end) [skip ci]; b84a550 chore: shipboard fixes, loop secret gate, public-readiness; d6b0d40 vault: curate journal 2026-06-13 via gpt-5.5 [skip ci].

↩ [[00-index]] · [[fazit-notes]]
