# 2026-06-13 — AI workflow journal

> Draft created by SessionEnd hook — CURATE before counting it as evidence.
> 3–6 honest lines. Contemporaneous or worthless.

- Delegated: a 5-agent parallel understand phase (serving contract, review-data source, brand, arc42 target, CI) and the merge-gate review (verifier + security-reviewer + codex gpt-5.5 + frontend code-reviewer). Implementation stayed in-orchestrator (Opus 4.8): the console is one tightly-coupled contract + brand layer, so parallel worktrees would have cost more than they saved.
- AI got wrong → caught by: (1) the previous AI scaffold's `lib/api.ts` used a camelCase, Java-flavoured contract (`id`, `taxPoints`, POST explain/coding-check) that renders every value as "—" against the real snake_case serving API. Caught by reading both sides of the wire, not by any test. (2) The brand was a teal placeholder with the visual law unimplemented; only reading the brand law and looking at the rendered UI catches that, so I implemented `.value-certified` / `.ai-content` from tokens.css. (3) The Next.js dev-mode badge leaked into the evidence screenshots; caught by eyeballing the captured PNGs (no test sees it), fixed with `devIndicators: false`.
- AI caught AI: codex (gpt-5.5) found a Critical the verifier and security pass under-weighted, the billing-field guard was client-side only, plus a High where the review BFF silently masked a configured upstream failure as success. Accepted and corrected.
- One concrete prompt → diff example: codex prompt "try to find what the first-pass reviews missed" → it flagged the client-only billing guard → diff added a server-side `BILLING_FIELDS` rejection in `app/api/review/route.ts` (commit 0524f9a).
- Decision made: owner chose (AskUserQuestion) to extend `apps/tarifguard` (not a new `-demo` dir) with a full truth-up; stale `apps/tarifguard-demo` path note corrected in AGENTS.md. Review write path is a console-owned mocked BFF pending the ingestion review endpoint (ADR-013 design scope); no new ADR needed. Shipped as PR #16, CI fully green.

↩ [[00-index]] · [[fazit-notes]]

<!-- session ended 09:55 · branch: main · last commit: d121353 chore(vault): journal + index (auto, session end) [skip ci] -->

<!-- session ended 09:56 · branch: main · last commit: ad3ff67 chore(vault): journal + index (auto, session end) [skip ci] -->

<!-- session ended 10:27 · branch: main · last commit: bfc170b chore(vault): journal + index (auto, session end) [skip ci] -->
