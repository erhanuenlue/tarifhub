# CAS grade estimate — TarifHub — 2026-06-14 (Block 3: Document + Fazit)

> This is an **estimate by an AI grade-auditor**, not the grader's verdict. It pairs the
> deterministic structural floor (`tools/cas_check.py`: 63/63 elements present, 0 regressions,
> 0 not-yet-due) with a judgment pass on what the floor cannot measure: prose quality,
> diagram/text consistency, the durchgängiger roter Faden, evidence honesty, and whether each
> criterion's **conjunctive** top-anchor genuinely holds. When in doubt between two tiers, the
> lower tier is reported and the gap to the higher named.

## Total estimate: **95 / 100**

Up **+1** from yesterday (94). The single change is **criterion 8**, which moves from
*überwiegend* (9) to *vollständig* (10): the one self-disclosed crack against "durchgehend
idiomatisch" — the console's Vitest/Playwright specs running as a CI no-op — is now closed.
Today's commit `163eda7` (PR #23, "fix three report-vs-repo contradictions … console tests …")
landed a real `"test": "vitest run && playwright test"` script in `apps/tarifguard/package.json`,
backed by **5 real test files** (`tests/unit/brand.test.tsx`, `tariff-card.test.tsx`,
`detail-panel.test.tsx`, `lib.test.ts`, and `e2e/smoke.spec.ts`), so the CI `console` job's
`npm run test --if-present` now executes the suite instead of skipping. §13's new "Console
component tests" section documents this honestly and consistently with the workflow.

The estimate still sits below 100 on exactly **one** judgment call, unchanged from yesterday and
owner-gated by design:

1. **Criterion 15 (−5):** the *Erklärung der Eigenständigkeit* is a **self-declared visible
   placeholder** ("It is not signed here, and the paraphrase below is not yet his words …
   Awaiting the owner's final text and signature"), with a blank Name/Ort/Datum/Unterschrift
   table. The 12-point anchor requires it *"gemäss Aufgabenstellung vollständig"*. By the
   conjunctive rule an unfinished, unsigned declaration drops the tier even though every other
   crit-15 anchor (per-phase Generierung/Review/Refactoring/Recherche structure, each claim tied
   to prompts/diffs/commit refs) is fully met. This is **intentionally** owner-gated by Fazit
   veto 3 ("Final acceptance is never delegated … the Erklärung der Eigenständigkeit is mine to
   sign") and only becomes final at submission, but it is the gating element today.

Everything else is judged at full marks against quoted evidence below.

## Per-criterion estimate

| # | Criterion | Tier | Est | Max | Conf | Why (anchor logic, quoted evidence) |
|---|---|---|---|---|---|---|
| 1 | Use-Cases & Anforderungen | vollständig | 5 | 5 | high | 5 Kernfunktionen as UCs (UC-01/03/04/06/09) each with Actor + Trigger + Outcome + `Realises` FR; supporting UCs; stakeholder table; UML use-case diagram (`docs/diagrams/use-cases.svg`). |
| 2 | NfA nach SMART | vollständig | 5 | 5 | high | 6 NfA, each with **Zielwert + Messverfahren + Measured + governing ADR** ("NfA-4 … p95 < 200 ms … measured 15.8 ms … ADR-002/006"). |
| 3 | Vision | vollständig | 5 | 5 | high | Problemstellung (§1) + Vision w/ Zielgruppe + Abgrenzung + one **KI-Nutzen sentence per Kernfunktion** ("UC-04 (serve deterministically): deliberately zero AI"). |
| 4 | Architektur bild+text | vollständig | 7 | 7 | high | C4 L1/L2 consistent w/ text; style **named + ADR-002**; roter Faden holds Problemstellung→freeze-line solution→Fazit; ≥10 ADRs. |
| 5 | Perspektiven | vollständig | 7 | 7 | high | Struktur (C4 component §5), Verhalten (runtime sequences + record-lifecycle states §6), Interaktion (OpenAPI + console contracts), each diagram + erläuternder Text. |
| 6 | Datenmodell | vollständig | 3 | 3 | high | ER diagram + `db/migrations/` + pgvector (HNSW/cosine) documented in §8/§10. |
| 7 | Code-Struktur | vollständig | 7 | 7 | high | Clean service split (ingestion/serving/mcp + console), per-service READMEs, sprechende Namen, Modulgrenzen consistent with §5 component view. |
| 8 | Framework-Konzepte | vollständig | 10 | 10 | high | **+1 today.** DI (`Depends`/`RepoDep`), env-only config (`get_settings()`), boundary `HTTPException(404)` + declarative 422s, validation — all idiomatic in code; framework choice justified; **console tests now wired into CI** (PR #23, `163eda7`), closing the last "durchgehend" crack. |
| 9 | Erkenntnisse | vollständig | 3 | 3 | high | `LEARNINGS.md`: every entry ends with PR + merge SHA and an explicit ✓/±/✗ disposition on the AI suggestion (e.g. "rejected ✗ … PR #5, ac8296c"). |
| 10 | Git-Repository | vollständig | 2 | 2 | high | Clickable repo URL in `docs/index.md`; full history; CI on every push. |
| 11 | Abnahmekriterien | vollständig | 5 | 5 | high | One G/W/T per UC, each with concrete observable + **Verifies (NfA)** cross-reference column; design-scope rows honestly labelled (UC-02 review POST). |
| 12 | Teststrategie | vollständig | 5 | 5 | high | Teststufen named + begründet; **"Tests der KI-Anteile"** with four named real families (guardrail boundary, gap-gate no-call, fill-reuse, fail-closed schema). |
| 13 | Unit-Tests | vollständig | 3 | 3 | high | Core logic + error paths covered (≥100 tests, error cases named); suite green in CI. |
| 14 | Test-Ergebnisse | vollständig | 3 | 3 | high | Pipeline/coverage output **quoted verbatim and interpreted** ("residual misses are bounded and named, not blind spots"); screenshots only illustrate. |
| 15 | KI-Werkzeuge | überwiegend | 7 | 12 | high | Per-phase structure + prompts/diffs/commits fully met; **−5**: Eigenständigkeitserklärung is a self-declared **placeholder**, not *vollständig* (owner-gated, Fazit veto 3). |
| 16 | KI-Services | vollständig | 6 | 6 | high | Two substantial AI roles (fill-only `ai_map` seam + e5 embedding/MCP search); guardrails (AST boundary, label law); Human-in-the-Loop review form. |
| 17 | Container-Sub-Systeme | vollständig | 5 | 5 | high | Compose + per-sub-system Dockerfiles + Helm; **distributed-services choice explicitly justified vs modular monolith via ADR-002**; oper/latency evidence captured in §7/§10. |
| 18 | Fazit | vollständig | 7 | 7 | high | Three concrete, evidenced vetoes (PR/commit refs: 057a6c1, 07cdfc5, PR #17) + honest limits + Übertrag ("match the working mode to the blast radius"). |

**Group subtotals (est / max):** Spezifikation 15/15 · Entwurf 17/17 · Programmierung **22/22** ·
Validierung 16/16 · KI und Architektur 25/30. **Total 95/100.**

## Gap list — ranked by points at risk

### 1. Criterion 15 — Eigenständigkeitserklärung is a placeholder (−5, the only deduction)
The §15 chapter is otherwise squarely at the 12-point bar: the four phases (Generierung /
Review / Refactoring / Recherche) are each present and **each claim is tied to a prompt line, a
file/diff, and a PR + commit SHA** (the fill-only prompt "set designation.fr/it/category ONLY
where the current value is None" → `057a6c1`; the Review section's rejected-P1/fixed-P2
double disposition `ac8296c`; the e5 prefix fix + recall@5 .833→.917). The single conjunctive
miss is the declaration itself, which the document openly marks:
> "Editorial note: this section is a visible placeholder … It is not signed here, and the
> paraphrase below is not yet his words … Awaiting the owner's final text and signature."
The 12-point anchor demands "Erklärung der Eigenständigkeit **gemäss Aufgabenstellung
vollständig**". A blank Name/Ort/Datum/Unterschrift table and a paraphrased-not-final body fails
"vollständig", so the tier drops to *überwiegend* (7). **This is by design owner-gated (Fazit
veto 3) and only becomes final at submission** — it is the single biggest correctable delta.
**Securing the 5 points:** owner authors and signs the final FFHS declaration verbatim before the
PDF hand-in; remove the placeholder banner.

### 2. No other tier is at risk; these are watch-items, not deductions
- **Crit 8 (resolved today):** keep `apps/tarifguard/tests` + `e2e/smoke.spec.ts` green and the
  CI `console` job actually executing them through to the submission build, so the §13 claim
  ("its tests wired in CI") stays true against the wire. The table line still reads
  `npm run test --if-present` — correct, since the script now exists and runs; do not let a
  later refactor delete the script and silently revert the job to a no-op.
- **Crit 11 / UC-02 / UC-09** honestly carry **partial / design-scope** status (review POST not
  implemented; explain endpoint "this release"). This honest framing is what keeps the tier; keep
  the status labels accurate as scope lands so the acceptance table never overstates.
- **Crit 14 / 17** evidence figures (coverage TOTALs, p95 15.8 ms, EAL/SL ingest counts) are
  dated 2026-06-13. Re-confirm they trace to the CI run linked in the PDF if any image changes
  before submission; graders run nothing, so every quoted figure must be reproducible from code.

## Deltas vs 2026-06-13
- **Crit 8: 9 → 10 (+1).** Console Vitest/Playwright specs now wired into CI via a real
  `package.json` `test` script + 5 test files (PR #23, `163eda7`); the self-disclosed "no-op"
  gap that capped crit 8 at *überwiegend* yesterday is closed. Programmierung group → 22/22.
- **Crit 15: unchanged at 7/12.** Eigenständigkeitserklärung still an unsigned placeholder
  (owner-gated, Fazit veto 3); no change today.
- **All other criteria: unchanged.** No regressions; floor still 63/63.
- **Total: 94 → 95 (+1).**

## Top three actions for the coming days
1. **Finalise and sign the Erklärung der Eigenständigkeit** (owner act, Fazit veto 3). This is now
   the **only** point swing left and the only thing between the current estimate and ~100/100.
   Replace the placeholder banner with the signed FFHS declaration before the PDF export.
2. **Lock the evidence numbers to the submission build**: re-confirm the coverage TOTALs, the p95
   latency, and the EAL/SL ingest counts quoted in §10/§14/§17 trace to the CI run linked in the
   PDF, so every quoted figure is reproducible from code alone (graders run nothing).
3. **Keep the console suite live through hand-in**: ensure the `console` CI job stays green and
   actually executes the 5 specs (do not let a refactor drop the `test` script and silently
   revert crit 8 to a no-op).

---
*This is an estimate produced by the grade-auditor, not the grader. The conjunctive rule was
applied strictly; where an anchor stood on an unfinished artefact (the unsigned declaration), the
lower tier was reported with the exact element that would secure the higher.*
