# CAS grade estimate — TarifHub — 2026-06-14 (Block 3: document + Fazit)

> **This is an estimate from the grade-auditor judgment tier, not the grader's verdict.**
> It sits on top of the deterministic structural floor (`tools/cas_check.py`: 63/63 elements present,
> 0 regressions, 0 not-yet-due). The floor proves elements *exist*; this pass judges only what it cannot:
> prose quality, diagram/text consistency, the durchgängiger roter Faden, evidence honesty, and whether
> each top anchor actually holds. Scoring is conjunctive: a level is awarded only if **all** of its anchors
> hold. This pass reconciles the morning judgment run with the same-day Codex (gpt-5.5) second opinion
> (`codex-second-opinion-2026-06-14.md`) and re-checks Codex's three disagreements against the source files.
> Where Codex was right, the score moved down; where its severity was too high, the reason is recorded.

## Total estimate: 94 / 100

| Group | Estimate | Max |
|---|---|---|
| Spezifikation | 15 | 15 |
| Entwurf | 16 | 17 |
| Programmierung | 22 | 22 |
| Validierung | 16 | 16 |
| KI und Architektur | 25 | 30 |
| **Total** | **94** | **100** |

Down 1 from the morning's 95: crit 5 docked 1 for a real diagram/text contradiction the Codex second
opinion surfaced and I confirmed against `use-cases.puml`. Crit 10 is held conditional (owner-only public
flip), and crit 15 stays at the lower conjunctive tier (7, unchanged from the morning pass).

## Per-criterion estimate

| # | Criterion | Wt | Est | Tier — and why not the tier above | Quoted evidence |
|---|-----------|----|-----|-----------------------------------|-----------------|
| 1 | Use-Cases & Anforderungen | 5 | 5 | vollständig. 5 Kernfunktionen as a UC table with Actor + Trigger + Outcome + "Realises FR"; stakeholder table with concerns. The grading-anchor tables are widerspruchsfrei. **Watch:** the *rendered* use-case diagram contradicts the catalogue on UC-05 (see crit 5); the tables themselves do not, so I hold 5 but flag it. | "UC-04 … API consumer (PIS/HIS) … frozen record, served verbatim … FR-5 … live" |
| 2 | NfA nach SMART | 5 | 5 | vollständig. 6 NfA, each row Zielwert + Messverfahren + Measured + governing ADR. | "NfA-4 … p95 < 200 ms single-record … p95 **15.8 ms** … [ADR-002] … [ADR-006]" |
| 3 | Vision | 5 | 5 | vollständig. Clear Problemstellung; vision with Zielgruppe/Bedürfnis/Abgrenzung; one KI-Nutzen sentence per Kernfunktion. | "UC-04 (serve deterministically): deliberately zero AI … the determinism guarantee *is* the feature." |
| 4 | Architektur Bild+Text | 7 | 7 | vollständig. C4 L1+L2 consistent with text; style per ADR-002; ≥10 ADRs; roter Faden index→intro→strategy→decisions→fazit holds. | "Start with [Introduction & Goals], then the [Solution Strategy], then the [Decisions]." |
| 5 | Perspektiven | 7 | **6** | überwiegend→top boundary. All three perspectives (Struktur/Verhalten/Interaktion) are present with diagram + text, so this is **not** the 4-pt "two perspectives" tier. Docked 1 for a real consistency defect: the use-case diagram source marks UC-05 `<<designed>>` "specified, not yet implemented" while §1 marks UC-05 "live (this release)". A strict conjunctive read punishes diagram/text contradiction. | use-cases.puml: "UC-05 … `<<designed>>` … specified, not yet implemented (UC-02, UC-05)" vs §1: "UC-05 … live (this release)" |
| 6 | Datenmodell | 3 | 3 | vollständig. ER diagram, two-table model explained, migrations present, pgvector(1024) documented. | "`embedding vector(1024)` carries the pgvector HNSW cosine index … Canonical source of truth: `db/schema.sql`" |
| 7 | Code-Struktur | 7 | 7 | vollständig. Clean service split, sprechende Namen, component table maps each module to a responsibility consistent with the entwurf. | "Pipeline orchestrator … Fixed stage order `load → parse → map → … → audit`; pure function of sorted inputs" |
| 8 | Framework-Konzepte | 10 | 10 | vollständig. §8 quotes crit 8 verbatim; names DI (`Depends`), env-only config, Pydantic validation, uniform boundary error handling; framework choice justified (ADR-001 in report); console tests wired into CI close the last idiomatic crack. | "dependency injection via FastAPI's `Depends` graph … declarative 422s from Pydantic/`Query` validation, explicit `HTTPException` 404s" |
| 9 | Erkenntnisse | 3 | 3 | vollständig. LEARNINGS.md ≥8 entries, commit/PR refs, explicit accept/correct/reject dispositions. | "Codex's suggestion … became `test_model_contract.py` … (PR #12, `257c0e0`)" |
| 10 | Git-Repository | 2 | 2* | vollständig **conditional**. URL in report, source complete. *Drops to 0 if the repo is still private at submission — the public flip is an owner-only Block-3 action, not yet realised. | "the source code lives in the public Git repository at [github.com/erhanuenlue/tarifhub]" |
| 11 | Abnahmekriterien | 5 | 5 | vollständig. One Given/When/Then per Kernfunktion against a concrete observable; explicit **Verifies (NfA)** cross-reference column. | "**UC-04** … **Then** HTTP 200 … | NfA-1 (determinism), NfA-4 (read latency)" |
| 12 | Teststrategie | 5 | 5 | vollständig. Teststufen named + justified; dedicated "Tests of the AI components" with four named families and real test files. | "`test_fill_reuse.py` … `test_reuse_makes_zero_api_calls` … handling non-deterministic AI output" |
| 13 | Unit-Tests | 3 | 3 | vollständig. Large suite, named error cases, runs in CI build. | "test_get_unknown_returns_404 (`services/serving/tests/test_api.py`)" |
| 14 | Test-Ergebnisse | 3 | 3 | vollständig — the evidence standard is met exactly. Pipeline output quoted verbatim AND interpreted; coverage per module; screenshots demoted to illustration. | "The numbers below are the interpretation; the CI link and the screenshots in §7 only illustrate." |
| 15 | KI-Werkzeuge | 12 | **7** | **überwiegend, NOT vollständig.** Phase structure (Generation/Review/Refactoring/Research) is excellent, each claim evidenced with prompt+diff+PR+commit. But the **Erklärung der Eigenständigkeit is an explicit unsigned placeholder** — and there is a **second** placeholder in `tools/build_pdf.py` (`ERKLAERUNG`). The 12-pt anchor requires it "vollständig", so the tier caps at 7. A strict grader reading "not yet his words" could go lower; 7 is the optimistic read of the lower tier. | ai-tools.md: "> It is not signed here … > Awaiting the owner's final text and signature." + build_pdf.py: "Note: this page is a placeholder." |
| 16 | KI-Services | 6 | 6 | vollständig. ≥2 substantial KI roles (fill-only `ai_map` + e5/MCP search), guardrails (gap-gate, schema-constrained, AST boundary), Human-in-the-Loop review form. | "`ai_map` seam … The **only live-AI point** … structured output via `messages.parse`, deterministic fallback to `map_raw`" |
| 17 | Container-Sub-Systeme | 5 | 5 | vollständig. Clear boundaries consistent with the entwurf; runs under Compose + Helm/k3d; style justified (ADR-002/006); operational evidence captured in docs/img. | "docker-compose + Helm/k3d; container-first distribution evidence" |
| 18 | Fazit | 7 | 7 | vollständig. Three concrete, justified, evidenced vetoes; honest limits; specific transfer ("match the working mode to the blast radius of an error"). | "**Veto 3: Final acceptance is never delegated.** The go-live decision, the Moodle submission and the Declaration of Authorship stay with me … (PR #17)." |

**Sum: 5+5+5+7+6+3+7+10+3+2+5+5+3+3+7+6+5+7 = 94 / 100.**

## Gap list — ranked by points at risk

Points at risk = weight × fraction of the criterion not yet at top tier.

| Rank | # | Criterion | At risk | Why the tier above is not yet held |
|------|---|-----------|---------|-----------------------------------|
| 1 | 15 | KI-Werkzeuge | **5.0** | Eigenständigkeitserklärung is an unsigned placeholder in **two** places: `docs/method/ai-tools.md` and the LaTeX builder `tools/build_pdf.py` (`ERKLAERUNG`). "vollständig" fails the conjunctive rule, capping the 12-pt chapter at the 7-pt tier. Owner-gated by Fazit veto 3 — correctly unsigned today, but the 5 points do not bank until signed in both the markdown and the PDF builder, then rebuilt and inspected. |
| 2 | 10 | Git-Repository | **2.0** | URL is in the report, but the repo is private until the owner-only public flip. If not anonymously reachable at submission, this collapses 2→0. Conditional, not yet realised; the Codex second opinion could not verify anonymous access from its environment either. |
| 3 | 5 | Perspektiven | **1.0** | The use-case diagram source marks UC-05 as `<<designed>>`/not-implemented while §1 calls it live (this release). One stale status label inside the use-case evidence area; a strict grader can read it as a diagram/text contradiction. Fix the PUML stereotype + legend and re-render the SVG. |

Recoverable against the 94 estimate: ~8 points (5 + 2 + 1, with the remaining gap to 100 the realistic
conjunctive friction across 18 criteria) — but 7 of those 8 (crit 15 signature, crit 10 public flip) are
owner-gated by design and cannot be closed by AI.

**Non-scoring risk Codex flagged and I confirmed:** em-dashes survive into the built PDF from diagram
sources (`use-cases.puml`, `seq-review-freeze.puml`, `runtime-harmonise-freeze.svg`), not only from
`mkdocs.yml`/`brand.css`. This violates the no-em-dash owner law in PDF-bound text; strip before export.
A §5↔§6 phrasing tension on the review form (§5 "made operable" vs §6 "not yet implemented / design scope")
is reconcilable — both mean the UI route simulates a server-side freeze pending the ingestion endpoint —
but tightening §5's wording would remove the ambiguity a grader might trip on.

## Top three actions for the coming days

1. **Sign the Eigenständigkeitserklärung in both places (crit 15, +5).** Replace the placeholder in
   `docs/method/ai-tools.md` *and* the `ERKLAERUNG` block in `tools/build_pdf.py` with the owner's own
   final FFHS declaration text and signature, then rebuild and inspect the PDF. Highest points-per-effort
   on the board, and the one element AI must not author (Fazit veto 3).
2. **Flip the repo to public and verify anonymous access (crit 10, +2 protected).** Confirm
   `github.com/erhanuenlue/tarifhub` resolves with no login before hand-in; otherwise crit 10 silently
   zeroes at grading.
3. **Fix the UC-05 diagram contradiction and strip PDF em-dashes (crit 5, +1; report quality).** Change the
   `use-cases.puml` UC-05 stereotype/legend to match the live-this-release catalogue status, re-render the
   SVG, and sweep em-dashes out of the diagram sources before the single-PDF compile so the roter Faden and
   quoted evidence survive the markdown→PDF transform the floor does not check.

## Honest closing line

This is an estimate produced against the official anchors, not the grader's verdict; the floor confirms the
elements exist, but only the lecturer awards the conjunctive top tier — and the two points I am most
confident about are that crit 15 cannot bank its full 12 until the Eigenständigkeitserklärung is signed in
both the chapter and the PDF builder, and crit 10 is worth zero if the repository is private when it is read.
