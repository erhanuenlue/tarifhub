# Codex (gpt-5.5) independent document review, 2026-06-13

Block-07 final-assembly review. Reviewer: OpenAI Codex CLI (gpt-5.5), headless `codex exec`, ChatGPT Pro login, sandbox read-only (proposes, never edits). Input: the assembled arc42 chapters, the AI-tools chapter, the decision matrix, the journal excerpts, the Fazit, the criterion map and the German summary. The gitignored anker doc was deliberately withheld; the in-repo criterion map carries the checklist.

Raw output archived at `build/codex-doc-review.raw.md`. Every finding is dispositioned below before pre-flight. This disposition list is itself Review-phase evidence for the AI-tools chapter (criterion 15).

| # | Sev | Finding | Disposition |
|---|-----|---------|-------------|
| 1 | P1 | arc42/11 and arc42/12 are stubs ("Stub", "replace this stub"), breaking the roter Faden | **FIXED.** ch11: stub banner removed, completed to a 7-row honest risk register (engine parity, console test debt, search availability, FR trade-off, L2 scope added to the 2 existing). ch12: replaced the stub with a 25-term glossary grounded in §5/§6/§8. Drafted by an `implementer` (Opus 4.8), merge-gate reviewed. |
| 2 | P1 | criterion-map row 15 claims the Erklärung der Eigenständigkeit as evidence, but the chapter holds a placeholder awaiting the owner | **FIXED (clarified).** The placeholder is by owner decision (the owner authors and signs the final text, veto 3). Row 15 now reads "the Erklärung der Eigenständigkeit (reserved slot, final wording and signature by the author)". The chapter already marks it unsigned. Honest, not an overclaim. |
| 3 | P1 | criterion-map row 10 claims a public repo; the Fazit says visibility was left with the owner (contradiction) | **FIXED.** Row 10 now reads "Public visibility is enabled by the author at go-live (see Fazit, veto 3)", aligning the map with veto 3. The actual public-flip is the owner's go-live action; flagged in pre-flight as owner-gated. |
| 4 | P1 | arc42/05 claims Vitest+Playwright console tests "pass in CI", contradicting arc42/13 (no test script; --if-present no-op) and the journal | **FIXED.** arc42/05 reworded to the honest state: the specs exist under `apps/tarifguard/tests` and `e2e` but are not wired to a `package.json` test script, so CI lints/builds/typechecks and `npm run test --if-present` is a no-op; wiring is planned (ADR-013). Now consistent with §13 and the new §11 console-test-debt risk row. |
| 5 | P2 | NfA-4 sets a search-latency target (<500 ms) but only point-read latency is measured; compose search returns 501 | **REJECTED (already honest).** The NfA-4 "Measured" cell already states single-record measured (p95 15.8 ms) and search-latency "method-defined", not measured; the 500 ms is a Zielwert, not a claimed measurement. The 501-on-stub behaviour is documented in §10/§7 and the new §11 search-availability risk row. No overclaim to fix. |
| 6 | P2 | EAL live-run numbers (70.6 s, 0.0 %, sha f0e74874) lack a dedicated evidence file like SL | **REJECTED (documented elsewhere).** The EAL numbers are captured and interpreted in arc42/10 §"BAG Analysenliste (EAL)" with the confidence histogram (`docs/img/eal_confidence_hist_2026-01-01.png`) and cross-checked against the audit_log per the journal (2026-06-11). The live ingest cannot be re-run offline this session; a separate SL-style file is a nice-to-have, not a gap in the report. |
| 7 | P2 | ai-tools.md states "Fable 5 access ended on 22 June 2026", a future date (review date 13 Jun 2026) asserted as past fact | **FIXED.** Reworded to "Fable 5 ran the early blocks, and the orchestrator was pinned to Opus 4.8 for the later blocks per ADR-018"; the future-dated "ended" claim is removed. |
| 8 | P2 | "roughly 110 active tariff types", "20+ sources" (arc42/01, zusammenfassung) are uncited | **REJECTED (already qualitative).** Both figures are explicitly approximate ("rund 110", "more than 20") domain-framing estimates of the Swiss BAG/FOPH tariff landscape; a precise source inventory is out of scope for the problem frame. Left as hedged estimates. |
| 9 | P3 | German idiom: "das Mapping in das kanonische TariffRecord" is awkward | **FIXED.** Changed to "das Mapping in das kanonische Datenmodell `TariffRecord`" (avoids the type-name gender ambiguity). |

Clean checks Codex confirmed: zero em-dashes in the reviewed documents; criterion-map rows 1-9, 11-14, 16-18 evidence present at cited locations; roter Faden coherent apart from the stubs (now fixed); German summary idiomatic apart from finding 9 (now fixed).

Outcome: all four P1 findings fixed, both fixable P2/P3 fixed, two P2 rejected with reasons recorded. No open blocking findings remain before pre-flight.
