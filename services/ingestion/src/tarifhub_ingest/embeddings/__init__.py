"""Embedding interface + offline deterministic stub.

Production uses multilingual-e5 written to pgvector; the default stub is a pure,
offline hashing embedder so the suite runs with no model download and no network.
"""
