# Cockpit (post-submission design package)

> **Out of scope for the CAS submission.** This directory is post-submission scaffolding, the design package for a build-and-operate control plane (ADR-022), written after the graded deliverable was set. It is not part of the graded submission and is deliberately excluded from the documentation site (`exclude_docs: cockpit/` in `docs/mkdocs.yml`), so it never renders into the arc42 site. It is kept in-repo only to record forward design intent.
